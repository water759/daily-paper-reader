---
title: On the Inseparability of Instructions and Data in Shared-Embedding Sequence Models
title_zh: 共享嵌入序列模型中指令与数据的不可分离性
authors: "Dewank Pant, Shruti Lohani, Avijit Kumar"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27567v1"
tags: ["query:sec-cloud-ai"]
score: 9.0
evidence: 提示注入为顶级AI安全风险，证明防御不可能性
tldr: 提示注入是LLM集成应用的最大安全风险，所有现有防御均被攻破。本文在共享嵌入架构中从数学上证明完美防御不可能：通过形式化Prompted Action Model和定义语义忠实控制(SFC)，用三个定理（来源恢复不可能、控制路径暴露、有限覆盖不变性差距）证明SFC不可实现，并在真实分词器和模型上量化界限。该结果类比冯·诺依曼架构的代码数据混淆导致的缓冲区溢出，指出提示注入是结构性问题，无法被内部分类或对齐消除，必须采用架构级指令与数据通道分离，需要类似DEP、ASLR等分层防御。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27567v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1218, \"height\": 613, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27567v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1566, \"height\": 509, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27567v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1041, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27567v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 579, \"height\": 353, \"label\": \"Table\"}]"
motivation: 所有提示注入防御均被攻破，需证明其根本不可能性，揭示指令与数据未分离的结构原因。
method: 形式化Prompted Action Model，定义语义忠实控制(SFC)，通过三个数学定理在共享嵌入架构中证明SFC不可实现。
result: 证明SFC不可实现，量化总变差距离等界限，并在真实分词器和模型上测量验证。
conclusion: 提示注入是结构性问题，必须进行架构级指令与数据通道分离，不能仅靠内部对齐或分类解决。
---

## 摘要
提示注入是LLM集成应用的首要安全风险，但迄今为止提出的所有防御措施均已被攻破。我们证明这并非偶然：在缺乏强制控制-数据分离的共享嵌入架构中，完美的提示注入预防在数学上是不可能的。我们将提示系统形式化为提示动作模型，其输出包括控制权威动作：拒绝决策、工具授权、策略路由和内存写入。我们定义语义忠实控制（SFC），即此类行为仅取决于不可信输入的含义，而非其编码方式。然后，我们通过三个结果证明SFC在共享管道中无法实现：来源恢复不可能性（共享表示使得可信与不可信内容在统计上不可分离，受限于全变差距离）；控制路径暴露（不可信令牌通过决定输出的相同注意力值聚合进入控制相关计算）；以及有限覆盖不变性差距（有限训练无法证明无限语义等价类上的不变性）。我们基于生产级分词器和模型上的测量来量化每个量。该结果是结构性的，而非当前防御的缺陷。它反映了冯·诺依曼机器中代码-数据混淆导致缓冲区溢出，这类漏洞需要数十年分层防御（DEP、Write-XOR-Execute、ASLR、栈金丝雀，最终是内存安全语言）才能遏制，因为单一机制不够。其含义相同：仅靠更好的管道内分类或对齐无法消除提示注入。它需要指令和数据通道的架构分离。我们确定了根本原因及其所需的解决方案类别。

## Abstract
Prompt injection is the top security risk for LLM-integrated applications, yet every defense proposed so far has been broken. We prove this is not a coincidence: in shared-embedding architectures that lack enforced control-data separation, perfect prompt-injection prevention is mathematically impossible. We formalize prompted systems as Prompted Action Models whose outputs include control-authoritative actions: refusal decisions, tool authorization, policy routing, and memory writes. We define Semantic-Faithful Control (SFC), the property that such behavior depends only on the meaning of untrusted input, not on how it is encoded. We then prove SFC is unachievable within the shared pipeline, via three results: a provenance-recovery impossibility (shared representations make trusted and untrusted content statistically inseparable, bounded by total variation distance); control-path exposure (untrusted tokens enter control-relevant computation through the same attention value-aggregation that determines outputs); and a finite-coverage invariance gap (finite training cannot certify invariance over infinite semantic-equivalence classes). We ground each quantity in measurements on production tokenizers and models.   The result is structural, not a gap in current defenses. It mirrors the code-data confusion in Von Neumann machines that gives rise to buffer overflows, a vulnerability class that took decades of layered defenses (DEP, Write-XOR-Execute, ASLR, stack canaries, and ultimately memory-safe languages) to contain, because no single mechanism sufficed. The implication is the same: prompt injection cannot be eliminated by better in-pipeline classification or alignment alone. It requires architectural separation of instruction and data channels. We identify the root cause and the class of solution it demands.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：提示注入（Prompt Injection）是当前LLM集成应用的头号安全风险，尽管已有大量防御手段（指令微调、RLHF、分类器、困惑度滤波、sandwich防御、XML标签、表示工程、circuit breakers等），但所有防御均被攻破。论文追问：是否存在理论上完美的提示注入预防？
- **整体含义**：论文证明，在**共享嵌入架构**（指令和用户数据通过同一嵌入函数、共享注意力-残差管道处理）且缺少强制控制-数据分离的设计下，完美的提示注入预防在数学上不可能。这并非防御不足，而是架构本身的固有限制。该结论与冯·诺依曼架构中代码-数据混淆导致缓冲区溢出漏洞的结构性根源完全类比。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **形式化框架**：
  - **Prompted Action Model (PAM)**：定义为一个元组 \(M=(\Sigma, d, E, f, A)\)，其中 \(E: \Sigma \to \mathbb{R}^d\) 是共享嵌入函数，\(f: (\mathbb{R}^d)^* \to \Delta(A)\) 是模型函数，动作空间 \(A\) 包括令牌生成、工具调用、策略路由、内存写入等控制权操作。
  - **语义忠实控制 (Semantic-Faithful Control, SFC)**：要求控制权行为（拒绝、工具授权等）只依赖于不可信输入的语义内容，而不依赖于编码方式。形式化为：若 \(u \equiv_{\text{sem}} u'\)（语义等价），则 \(\Pi_{\text{ctrl}} Y(s\|u) = \Pi_{\text{ctrl}} Y(s\|u')\)。
- **三个核心定理**：
  1. **来源恢复不可能定理**（Theorem 3）：基于全变差距离（TV distance）证明，在共享表示下，可信与不可信内容的表示分布不可完全分离，最优贝叶斯误差严格大于0。
  2. **控制路径暴露定理**（Theorem 4）：证明不可信内容通过注意力值聚合进入控制相关节点（如生成位置隐藏状态），这些节点直接影响控制权输出。
  3. **有限覆盖不变性差距引理**（Lemma 5）：训练或对齐只能覆盖有限个语义等价类成员，无法保证对所有无限变体的不变性。
- **主定理（Theorem 6）**：结合以上三点，证明共享表示管道中无法保证完美SFC。推论：完美提示注入预防在共享嵌入架构中不可能。
- **证明方法**：结合贝叶斯最优误差理论、注意力机制分解、有限覆盖不变性论证、以及表示级到行为级传播的桥接命题（Proposition 8）。

### 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法
- **实验内容**（非传统基准测试，而是量化理论量的实证支撑）：
  - **M1 – 词汇重叠测量**：使用四个公开系统提示仓库（TheBigPromptLibrary等）作为指令语料，UltraChat-200K中的50,000个用户轮次作为用户数据语料。测量三种分词器（Mistral-7B 32K、Llama-3.1 128K、Qwen-2.5 152K）的词汇重叠指标（Jaccard类型重叠、概率质量重叠、指令覆盖率）。
  - **M2 – 表示层重叠测量**：在Llama 3.1 8B Instruct（8位量化）上，从每类各300个序列中提取第0、8、16、24、31层的平均池化隐藏状态，计算MMD（最大均值差异）并附bootstrap 95%置信区间（B=1000）。
  - **M3 – 行为SFC违反测试**：构造金融系统提示（含严格拒绝规则）和基准投资查询，然后应用5种提示注入技术（DAN角色覆盖、many-shot、假管理员、Crescendo、虚拟化/角色劫持），测试Llama 3.1 8B Instruct和Mistral 7B Instruct v0.2，使用贪心解码（最多350新令牌）。观察拒绝决策是否改变、输出结构是否变化。
  - **M4 – 权重手术确认：Abliteration**：引用社区已知技术（通过移除残差流中单个拒绝方向显著降低拒绝能力），与论文的暴露控制和有限覆盖论点一致，作为经验动机。
- **没有标准benchmark**：论文是非传统实证型，实验设计为支撑理论量而非与现有方法对比。

### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量、训练时长等算力信息。仅提到Llama 3.1 8B Instruct使用8位量化，M1和M2实验基于公开HuggingFace模型；M3实验使用贪心解码，推理开销小。整体算力需求较低。

### 5. 实验数量与充分性
- **实验数量**：四组实验（M1–M4），每组各有具体测量。M1覆盖3个分词器，M2涵盖5个层级（每层300+300样本），M3涉及2个模型×5种攻击（但所有攻击均未导致拒绝翻转，仅观察到输出结构变化），M4引用外部结果，未做新实验。
- **充分性评估**：
  - **充分方面**：M1和M2提供了支撑Lemma 1和Assumption 3的定量证据；M3直接展示控制路径暴露（Theorem 4）；M4加固了论证。实验设计紧扣理论，非冗余。
  - **不充分方面**：M3的测试结果未产生拒绝翻转（仅改变了输出长度、前导语等），作者承认并非严格SFC违反，而是控制路径污染的间接证据。实验覆盖模型少（仅2个），攻击种类有限，且未测试更复杂的对抗采样或工具调用场景。整体实验规模偏小，更偏向概念验证而非全面基准。

### 6. 论文的主要结论与发现
- **核心结论**：在共享嵌入且缺乏强制控制-数据分离的架构中，完美提示注入预防不可能。该结论通过三个定理严格证明，而非经验观察。
- **关键发现**：
  - 三个必要性条件（共享嵌入、暴露控制路径、有限覆盖）共同导致不可行性。
  - 结构类比冯·诺依曼架构中缓冲区溢出，提示注入是“代码-数据混淆”在神经网络中的对应问题。
  - 单一机制（如内部分类或对齐）无法解决，需要架构级分离（如独立编码器、类型化注意力、不可变来源标签）配合分层防御（运行时缓解、沙箱等）。
- **实际意义**：组织应假设提示注入必然发生，实施深层防御，限制爆炸半径，不信任任何单一护栏。

### 7. 优点
- **理论贡献突出**：首次给出提示注入的严格不可能性证明，将问题从“如何防御”提升到“为何根本不可能完美防御”，具有重要理论价值。
- **形式化严谨**：清晰定义了PAM、SFC、来源恢复不可能的统计界限、控制路径暴露、有限覆盖不变性差距，每一步证明数学可验证。
- **跨学科类比深刻**：与冯·诺依曼架构的类比不仅生动，而且反向指导了解决方案方向（即架构分离），提升论文的影响力。
- **实证连接理论**：M1–M4实验直接量化理论量（词汇重叠、MMD、行为变化、abliteration），使抽象理论接地气，避免纯数学空谈。
- **诚实讨论局限性**：明确承认SFC的实践难度（如M3未观察到直接拒绝翻转），声明泛型假设的必要性和合理性，并指出超出范围架构（分离编码器、类型注意力、外部引擎）。

### 8. 不足与局限
- **实验覆盖不够全面**：仅测试了两个模型（Llama 3.1 8B Instruct和Mistral 7B Instruct v0.2），且都是较小规模。未覆盖更大模型（如70B、GPT-4级别）或不同系列（Gemma、Qwen等）。
- **M3实验未直接证明SFC违反**：所有攻击未能使模型违反拒绝指令，只观察到输出结构变化，作者承认未达到定理中的“控制权行为改变”标准。理想实验应包含成功反转拒绝的GCG后缀等已知攻击。
- **依赖于几项假设**：Assumptions 1-3（控制路径表达性、局部非退化、表示非不相交）虽合理且部分得实证支持，但在极端参数配置或特定截断训练下可能不成立，限制了定理的普遍性。
- **未定量评估有限覆盖差距的实际大小**：Lemma 5仅指出理论上存在未覆盖元素，但未给出实际攻击者可利用的变体数量或难度估计。
- **与应用场景存在距离**：结论适用于agentic系统（含工具、内存等），但大多数当前LLM应用尚不完整具备这些功能。论文对普通聊天模型（无工具调用）的结论较弱。
- **解决方案方向缺乏具体实现**：提出需要架构分离，但未给出可直接工程化的设计细节（如ASIDE等已有初步工作，但未深入比较或改进）。

（完）

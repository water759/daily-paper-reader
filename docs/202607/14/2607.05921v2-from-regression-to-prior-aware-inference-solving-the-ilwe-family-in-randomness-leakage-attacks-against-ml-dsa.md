---
title: "From Regression to Prior-Aware Inference: Solving the ILWE Family in Randomness Leakage Attacks against ML-DSA"
title_zh: 从回归到先验感知推理：解决针对ML-DSA的随机性泄漏攻击中的ILWE家族
authors: "Peiheng Zhang, Yuejun Liu, Wei Cheng, Muye Li, Honglin Shao, Yongbin Zhou"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05921v2"
tags: ["query:sec-cloud-ai"]
score: 9.0
evidence: ML-DSA随机性泄露导致密钥恢复
tldr: ML-DSA签名安全性依赖于随机性，但实际实现可能泄露随机性导致密钥恢复。现有攻击主要构建ILWE类问题，求解阶段未被充分探索。本文提出统一框架，系统评估多种求解器在三种ILWE实例上的性能，发现先验感知离散推理在FS-ILWE上大幅降低所需关系数量。该工作为随机性泄露攻击提供了系统评估和新基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05921v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1688, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1513, \"height\": 1226, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1697, \"height\": 747, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 929, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1702, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 931, \"height\": 1214, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05921v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05921v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 691, \"height\": 160, \"label\": \"Table\"}]"
motivation: 现有随机性泄露攻击注重构造ILWE实例，但对求解器的影响研究不足，缺乏统一比较框架。
method: 提出统一框架，涵盖普通ILWE、FS-ILWE和CILWE实例，评估三类求解器（OLS、BP、先验感知离散推理）。
result: 先验感知离散推理在FS-ILWE上相比基线减少10-74倍关系数，优于最小二乘和置信传播。
conclusion: 系统评估了不同求解器对密钥恢复效率的影响，为ML-DSA安全性分析提供新基准。
---

## 摘要
ML-DSA 是由 NIST 标准化的代表性格基签名方案。它依赖于签名随机性和拒绝采样，以确保发布的签名在统计上与密钥无关。然而，实际实现可能泄漏有关该随机性的部分信息，这种泄漏可以将公开签名转化为 ILWE 类型的问题，导致密钥泄露风险。这种随机性泄漏攻击可以形式化为两阶段密钥恢复过程：首先将泄漏的部分信息和公开签名转化为 ILWE 家族实例，然后应用恢复求解器恢复密钥。现有工作主要集中在第一阶段，即在不同的泄漏模型下构建此类实例。相比之下，求解器在后续实例求解阶段的作用仍未被充分探索，现有攻击通常依赖于针对特定模型的专用求解器。为了解决这一问题，我们提出了一个统一的框架，用于系统评估在泄漏衍生的 ILWE 家族实例上的不同恢复求解器。该框架涵盖了三种 ILWE 实例，包括普通 ILWE、Fiat-Shamir ILWE（FS-ILWE）和隐藏 ILWE（CILWE），涵盖不同场景。在我们的框架内，我们探索了三类求解器。我们的实验表明，求解器对密钥恢复效率有显著影响。特别是在 FS-ILWE 上，与基线相比，先验感知离散推理将信息关系的数量减少了一到两个数量级：与 OLS 相比，BP 在无噪声设置下减少了 15.4 倍到 64.9 倍，在有噪声设置下减少了 10.5 倍到 73.9 倍。总体而言，这项工作提供了对随机性泄漏攻击中不同求解器的系统评估，并为未来对 ML-DSA 的分析提供了新的基准。

## Abstract
ML-DSA is a representative lattice-based signature scheme standardized by NIST. It relies on signing randomness and rejection sampling to ensure that released signatures are statistically independent of the secret key. Practical implementations, however, may leak partial information about this randomness, and such leakage can transform public signatures into ILWE-type problems, resulting in secret key disclosure risks.   Such randomness leakage attack can be formulated as a two-stage key-recovery procedure, in which leaked partial information and public signatures are first transformed into an ILWE-family instance, and then a recovery solver is applied to recover the secret key. Existing work has mainly focused on the first stage by constructing such instances under different leakage models. By contrast, the role of solver in the subsequent instance-solving stage remains under-explored, and existing attacks often rely on ad-hoc model-specific solvers. To address this gap, we propose a unified framework to systematically evaluate different recovery solvers on leakage-derived ILWE-family instances. The framework covers three ILWE instances, including the ordinary ILWE, Fiat-Shamir ILWE (FS-ILWE) and Concealed ILWE (CILWE) under different scenarios.   Within our framework, we explore three classes of solvers. Our experiments show that the solver has a significant impact on the secret-key recovery efficiency. In particular, on FS-ILWE, prior-aware discrete-inference reduces the number of informative relations by one to two orders of magnitude compared to the baselines: Compared with OLS, BP constitutes a reduction by a factor of 15.4x-64.9x in noise-free settings, and by a factor of 10.5x-73.9x in noisy settings. Overall, this work provides a systematic evaluation on different solvers in randomness leakage attacks, and presents new benchmarks for future analysis on ML-DSA.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：ML-DSA（CRYSTALS-Dilithium）是 NIST 标准化的后量子数字签名方案，其安全性依赖于签名随机性 `y` 和拒绝采样，使得公开签名与密钥无关。但实际实现可能通过侧信道（如功耗、微架构）泄漏随机性的部分信息，导致签名转化为整数学习误差（ILWE）类型问题，从而引发密钥恢复风险。
- **背景与问题**：现有随机性泄漏攻击可分为两阶段：第一阶段将泄漏信息与公开签名组合成 ILWE 家族实例（如普通 ILWE、Fiat-Shamir ILWE (FS-ILWE)、隐藏 ILWE (CILWE)）；第二阶段应用求解器恢复密钥。但现有工作主要关注第一阶段（实例构建），而对第二阶段求解器的作用缺乏系统性研究，且不同攻击往往采用专用求解器（如 OLS、健壮回归等），缺少统一比较框架。
- **整体含义**：本文填补了这一空白，通过统一框架系统评估三类求解器在三种 ILWE 实例上的性能，揭示求解器选择对密钥恢复效率的关键影响，为未来 ML-DSA 安全性分析提供新基准。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出统一框架，将三种 ILWE 实例（OILWE、FS-ILWE、CILWE）纳入同一问题表达式 `b = As + e`，并基于此框架对比三类求解器：最小二乘回归（least-squares）、健壮回归（robust regression）、先验感知离散推理（prior-aware discrete inference）。
- **关键技术细节**：
  - **最小二乘回归求解器**：包括 OLS、SVD、QR、GD-MLE，它们通过最小化 `∥Ax − b∥₂²` 得到实值估计，再逐坐标四舍五入到整数。理论阈值由 `∥s − ŝ∥∞ < 1/2` 决定。
  - **健壮回归求解器**：包括 Huber 回归、Cauchy 回归，通过替代损失函数（如 `ℓ₁`、Huber、Cauchy）降低异常样本的影响，但仍需先求出实值估计再舍入。
  - **先验感知离散推理求解器**：
    - **置信传播 (BP)**：将每个密钥系数视为离散随机变量，通过因子图上的消息传递（变量到因子、因子到变量）近似边缘后验，最终采用最大后验（MAP）准则输出整数密钥。
    - **目标诱导贪婪搜索 (Greedy)**：维护一个整数候选，每次选择使残差损失（如 `ℓ₂`）下降最大的坐标更新，搜索空间限于有限整数域 `S`。
    - **有界误差爬山 (Hill-climbing)**：基于有效误差区间 `[−β_eff, β_eff]` 对候选评估一致性，选择使区间外残差总和最小的更新。
  - **公式 / 算法流程说明**：BP 的消息更新通过动态规划（卷积）在支持宽度上多项式时间完成；Greedy 的评分函数为候选更新前后损失差值；Hill-climbing 评分函数为 `Σ max(0, |r_i(x)| − β_eff)`。

#### 3. 实验设计：数据集/场景、benchmark、对比方法
- **数据集/场景**：
  - OILWE：随机生成 `n=256/512`、`α_a∈{1,5}`、`α_e∈{78,120,196}` 的样本，秘密稀疏（15% ±1、15% ±2、其余 0）。
  - FS-ILWE：基于 ML-DSA 三个安全级别（ML-DSA-44/65/87）的真实参数，泄漏位置 `j=4~7`（或 5~8），分别测试无噪声和噪声（比特翻转概率 `p_flip` 从 0 到 0.45）。
  - CILWE：同样基于 ML-DSA 参数，生成混合样本：以概率 `1−p_con` 为零误差样本（`y_i=0`），以概率 `p_con` 为隐蔽样本（`y_i` 随机），`p_con` 范围 0~0.9。
- **Benchmark**：以 OLS（[7-9]）作为 FS-ILWE 基线，以 OLS 和健壮回归作为 CILWE 基线（[6]）。
- **对比方法**：
  - 最小二乘类：OLS、SVD、QR、GD-MLE。
  - 健壮回归类：`ℓ₁`-LP、Huber、Cauchy。
  - 先验感知离散推理类：BP、Greedy-`ℓ₂`、Hill-climbing。
  - 所有求解器使用相同的样本序列，最小样本数通过二分搜索确定。

#### 4. 资源与算力
- **文中未明确说明**：论文没有提及所使用的 GPU 型号、数量、训练时长等算力信息。只提到实验在五个不同随机种子下运行，使用相同序列。因此，无法量化计算资源消耗。

#### 5. 实验数量与充分性
- **实验数量**：
  - OILWE：5 种参数组合 × 3 类方法 × 5 个种子 → 大量测试，平均最小样本数。
  - FS-ILWE：3 个 ML-DSA 级别 × 4 个泄漏位置 × 5 个种子，以及噪声设置下多个 `p_flip` 值。
  - CILWE：3 个 ML-DSA 级别 × 多个 `p_con` 值（0~0.9） × 5 个种子。
- **充分性**：
  - 实验覆盖了三种 ILWE 实例和所有代表性求解器，参数范围较广（不同安全级别、泄漏位置、噪声率、隐藏率）。
  - 使用相同样本序列确保公平比较，并通过二分搜索准确找到最小关系数。
  - 但缺少对计算时间、内存消耗的对比，也缺少对更大规模参数（如更高维数）的验证。总体来说，实验设计客观、公平，足以支撑核心结论。

#### 6. 论文的主要结论与发现
- **求解器选择显著影响密钥恢复效率**：先验感知离散推理（特别是 BP）在多数场景下大幅减少所需关系数。
- **FS-ILWE**：
  - 无噪声下，BP 比 OLS 减少 15.4×～64.9× 的关系需求；比 Hill-climbing 减少最多 7.6×。
  - 有噪声下，BP 比 OLS 减少 10.5×～73.9× 的关系需求，且能恢复更高翻转率（`p_flip=0.45`）的实例。
- **CILWE**：
  - 隐藏率 `p_con < 0.9` 时，BP 比 Cauchy 回归更高效；`p_con ≥ 0.9` 时，Cauchy 回归更稳定。
- **OILWE**：BP 比 OLS 减少 4.2×～56.1× 的样本需求。
- 健壮回归在普通 ILWE 上无优势，但在隐蔽样本占优的 CILWE 高隐藏率下表现最好。
- 最小二乘类求解器（OLS、SVD、QR、GD-MLE）和 Greedy-`ℓ₂` 效果几乎一致，因为它们基于相同 `ℓ₂` 目标。

#### 7. 优点：方法或实验设计上的亮点
- **统一框架**：首次将三种 ILWE 实例纳入同一问题表达式，系统比较不同求解器，填补了第二阶段研究的空白。
- **求解器分类全面**：覆盖从连续回归到离散推理的三大类，并详细解释其工作原理和适用条件。
- **实验设计严谨**：使用相同样本序列、二分搜索确定最小样本数、多次取平均，确保比较公平。
- **新发现**：揭示了先验感知离散推理（BP）在多种泄漏场景下的显著优势，以及健壮回归在高隐藏率下的补位作用。
- **实用性强**：为 ML-DSA 随机性泄漏攻击的实际有效性提供了定量基准，指导防御者选择合适的泄漏防护策略。

#### 8. 不足与局限
- **实验覆盖**：仅针对 ILWE 线性系统，未考虑更复杂的非线性泄漏模型。未对比整数线性规划（ILP）等现有求解器（文中提到 ILP 但未纳入统一实验）。
- **计算效率**：未报告求解器的实际运行时间或内存开销，先验感知离散推理（如 BP）在稀疏图上可能仍有较大计算复杂度，但未量化。
- **参数局限性**：实验参数（如 `n=256`、`α_a=1/5`）较限于 ML-DSA 的典型值，未扩展至更大维度或更极端误差。
- **噪声模型**：FS-ILWE 的噪声仅考虑独立比特翻转，未涵盖更复杂的侧信道噪声分布。
- **CILWE 隐藏率范围**：仅测试到 `p_con=0.9`，更高隐藏率（>0.9）下 Cauchy 是否仍有效未明确。
- **应用限制**：所有实验基于模拟数据，未在实际侧信道泄漏上进行验证。

（完）

---
title: "SABLE: Minimalist Instruction-Level Authenticated Encryption for Constrained Confidential Computing"
title_zh: SABLE：面向受限机密计算的极简指令级认证加密
authors: "Hamid Noori, Carlton Shepherd"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16771v1"
tags: ["query:sec-cloud-ai"]
score: 9.0
evidence: 提出了SABLE，一种面向机密计算的指令级认证加密RISC-V架构
tldr: 传统处理器执行时指令以明文存在，易遭攻击。本文提出SABLE，一种与微架构无关的最小侵入式RISC-V指令级加密验证架构，仅需对编译后的ELF文件稍作后处理。在指令存储器包装器和CPU前端集成解密验证阶段，探索了七种微架构（单周期至多周期），使用ASCON-128a在FPGA上实现。相比基线，面积开销1.6-9.3倍，性能下降4.1-10.0倍，功耗增加1.5-8.0倍，每指令能量开销10.4-80.0倍。该设计为受限机密计算提供了灵活的安全方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16771v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 872, \"height\": 846, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16771v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16771v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 1216, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16771v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1851, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16771v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 907, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16771v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 849, \"height\": 203, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16771v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 817, \"height\": 263, \"label\": \"Table\"}]"
motivation: 现有指令级加密方案依赖特定微架构、事后检测或非标准密码，缺乏通用且低侵入的解决方案。
method: 提出SABLE架构，在指令存储器包装器和CPU前端集成解密验证模块，支持单周期/多周期微架构，兼容标准RISC-V工具链。
result: FPGA实现七种变体，LUT开销1.6-9.3倍，性能下降4.1-10.0倍，功耗增加1.5-8.0倍，每指令能耗增加10.4-80.0倍。
conclusion: SABLE提供与微架构无关的指令级加密，通过设计点权衡面积、性能和能耗，适用于资源受限的机密计算。
---

## 摘要
传统处理器设计在执行过程中将代码和数据以明文形式暴露，使其本质上容易受到恢复知识产权或修改安全/安全检查的攻击。指令级加密（ILE）能够在运行时对单个加密的程序指令进行CPU级别的解密、执行以及可选认证。然而，现有方案依赖于特定的微架构，在指令执行后才检测损坏的指令，采用非标准密码，或需要对程序状态进行复杂分析。在本工作中，我们介绍并提出了一种RISC-V处理器架构（SABLE）的设计探索，该架构实现了对程序的最小侵入性指令级认证加密。SABLE与底层微架构无关，与标准RISC-V工具链兼容，只需对编译后的ELF二进制文件进行少量后期处理。我们在两个点（指令内存包装器和CPU前端）集成了解密与验证阶段，并探索了从单周期（组合逻辑）设计到六种多周期（时序）变体的七种ILE微架构。我们使用ASCON-128a在Xilinx Artix-7 FPGA上实现了这些设计，并采用开源NEORV32片上系统进行评估。相对于基线性能，这些配置在使用Dhrystone基准测试套件时，其LUT、性能、功耗和每指令能耗开销分别为1.6-9.3倍、4.1-10.0倍、1.5-8.0倍和10.4-80.0倍。最后，我们讨论了设计权衡，强调了面积、性能和能耗感知的设计点。

## Abstract
Conventional processor designs expose code and data as plaintext throughout execution, rendering them inherently vulnerable to attacks that recover intellectual property or modify security/safety checks. Instruction-level encryption (ILE) enables CPU-level decryption, execution, and optionally authentication of individual encrypted program instructions at runtime. However, existing proposals depend on specific micro-architectures, detect corrupted instructions after they have executed, rely on non-standard ciphers, or require complex analyses of program state. In this work, we introduce and present a design exploration of a RISC-V processor architecture (SABLE) that enables minimally invasive instruction-level authenticated encryption of programs. SABLE is agnostic to the underlying micro-architecture, remaining compatible with the standard RISC-V toolchain with minor changes to post-process compiled ELF binaries. We integrate a decrypt-and-verify stage at two points (the instruction-memory wrapper and the CPU frontend) and explore seven ILE micro-architectures from a single-cycle (combinational) design to six multi-cycle (sequential) variants. We implement and evaluate the designs using ASCON-128a on a Xilinx Artix-7 FPGA with the open-source NEORV32 system on chip. Relative to baseline performance, the configurations span LUT, performance, power, and energy-per-instruction overheads of 1.6-9.3$\times$, 4.1-10.0$\times$, 1.5-8.0$\times$, and 10.4-80.0$\times$, respectively, using the Dhrystone benchmarking suite. Finally, we discuss design trade-offs, highlighting area-, performance-, and energy-aware design points.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：传统嵌入式处理器在执行过程中，指令以明文形式存在于内存和总线上，容易遭受固件提取、代码篡改和逆向工程等攻击。现有指令级加密（ILE）方案存在诸多局限：依赖特定微架构、采用非标准密码、在指令执行后才检测损坏（事后检测）、需要复杂的程序控制流图分析或深刻的工具链修改。
- **研究动机**：需要一种**微架构无关、支持预执行认证、单层原语、且对工具链侵入性小**的指令级认证加密方案，以保护受限嵌入式设备中的固件机密性和完整性。
- **整体含义**：论文提出了SABLE架构，它利用轻量级AEAD算法ASCON-128a，对每个RISC-V指令单独进行认证加密，在指令获取阶段（而非执行后）验证完整性，并在FPGA上进行了全面的设计空间探索，为受限机密计算提供了可权衡面积、性能和能耗的灵活方案。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在RISC-V处理器中插入一个对微架构透明的认证解密模块，对每条指令独立进行ASCON-128a解密和标签验证，失败则立即触发异常，阻止篡改指令执行。
- **关键技术细节**：
  - **密码原语**：采用NIST标准化轻量级AEAD算法ASCON-128a，状态320位（5个64位字），密钥128位，nonce 128位，标签128位。针对固定32位密文（单条指令）和零关联数据进行优化。
  - **集成点**：解密验证模块可集成在**指令存储器（IMEM）包装器**或**CPU前端**（指令预取缓冲区之前）。IMEM集成可保护只读数据，但明文在总线上；前端集成则密文在总线上，更安全。
  - **地址绑定nonce**：每条指令的nonce由基nonce的低32位与取指地址（右移2位）相加得到（公式1），防止重放和拼接攻击。
  - **标签存储**：每条指令附带128位标签，存储在独立的标签存储器（TAGMEM）中，容量为指令内存的5倍。
  - **多周期变体**：将ASCON-p[12]置换折叠为不同轮回/周期（12,6,4,3,2,1轮/周期），实现面积、性能、功耗的权衡。共设计了MCP12至MCP1六种多周期实现。
  - **工具链适配**：仅修改objcopy，将编译后的ELF二进制中.text段每个指令用ASCON-128a加密，并生成对应的.text.tag段存放标签，无需修改编译器或汇编器。还扩展了Spike ISA模拟器以支持加密执行做参照。

### 3. 实验设计：使用了哪些数据集/场景，benchmark是什么，对比了哪些方法

- **测试程序**：`hello_world`（610条静态指令，执行822条）和`Dhrystone`（10,000次迭代，1214条静态指令，执行约454万条）。
- **基准（Baseline）**：未加密的NEORV32处理器（RISC-V软核），不使用任何ILE。
- **对比方法**：论文未在实验中直接与其他ILE方案（如SCFP、POLYGLOT等）进行性能对比，而是在表1中进行了定性比照；实验主要对比的是SABLE自身不同设计变体之间的差异，包括：
  - 单周期：IMDEC（加密-only，IMEM集成）、IMAUDC（认证，IMEM集成）、FEDEC（加密-only，前端集成）、FEAUDC（认证，前端集成）。
  - 多周期：FEAUDC-MCP12至MCP1（6种，均为认证前端集成）。
- **评估指标**：LUT数、寄存器数、BRAM数、关键路径延迟、时钟周期时间（CCT）、CPI、性能开销（相对基线）、功耗、每指令能量（EPI）。全部归一化到基线。

### 4. 资源与算力：使用了多少算力（GPU型号、数量、训练时长等）

- 论文**没有使用GPU**，实验在**Xilinx Artix-7 FPGA（Nexys A7-100T）** 上进行合成与实现。工具为Vivado ML 2025.2。设计以VHDL描述，无需训练，因此不涉及GPU算力。若需类比，可认为FPGA逻辑资源约15,600 LUT、8块BRAM（用于TAGMEM）等。文中未报告具体的综合时长或仿真耗时。

### 5. 实验数量与充分性：做了多少组实验，是否充分、客观、公平

- **实验组数**：共配置了**11种设计变体**（基线 + 4种单周期 + 6种多周期），在2个测试程序上进行了功能验证和性能测量。此外进行了负测试（故意破坏指令/标签验证异常）。
- **充分性**：全面覆盖了设计空间，包括单/多周期、加密/认证、不同集成点，并提供了面积、性能、功耗、能量等多维度对比。消融分析（单周期vs多周期、认证vs加密）充分。
- **公平性**：所有设计在相同FPGA平台上、使用相同工具链进行综合，且通过Spike ISA模拟器、GHDL仿真和FPGA硬件三级验证，确保功能正确。未与其他实际ILE方案进行同平台对比，但定性讨论合理。
- **客观性**：指标报告清晰，且给出了每指令能量（EPI）这一归一化指标，便于跨时钟域比较。

### 6. 论文的主要结论与发现

- SABLE能够以可接受的硬件开销实现每指令的认证解密，在确定性的预执行检测上优于隐式链式方案（如SCFP）。
- 设计空间存在三个不同最优：**MCP1**最小面积（1.6× LUT）和最低功耗（1.5×），**MCP3**最佳性能开销（4.1×），**MCP2**最低每指令能耗（10.4×）。单周期认证设计（FEAUDC）面积开销9.3×，性能开销10×，功耗8×，EPI 79.7×。
- 多周期折叠显著降低了EPI（从79.7×降至10.4×），使能效提升近一个数量级。
- 标签存储带来5倍指令内存开销，这是确定性认证的固有代价。

### 7. 优点：方法或实验设计上的亮点

- **微架构无关性**：解密验证模块独立于处理器流水线，可移植到不同RISC-V核。
- **预执行检测**：在指令解码和执行之前即触发异常，防止任意伪随机指令执行，安全性更强。
- **单层原语**：同时提供机密性和完整性，无需多层叠加。
- **工具链侵入极小**：仅需修改objcopy，不改编译器、汇编器或链接器，兼容标准ELF。
- **全面设计空间探索**：从单周期到1轮/周期的多周期变体，给出了明确的面积-性能-功耗-能量权衡曲线。
- **多级验证**：ISA模拟器（Spike）、RTL仿真（GHDL）、FPGA硬件三阶段验证，确保正确性。
- **负测试**：故意注入错误指令和标签，验证了异常机制有效。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖有限**：仅对NEORV32单一处理器进行实现，未在其他RISC-V核（如Rocket、BOOM）或ASIC上验证泛化性。测试程序仅两个轻量级基准，未涉及操作系统或复杂应用。
- **高开销**：即使最优设计（MCP2）也引入10.4× EPI开销，对于极低功耗设备可能仍难以接受。标签存储增加5倍IMEM，在内存受限平台不适用。
- **不支持16位压缩指令**：仅支持32位固定宽度指令，与RISC-V标准压缩指令集不兼容。
- **无控制流完整性（CFI）**：声明不覆盖代码复用攻击，需要额外机制（如阴影栈）补充。
- **无数据保护**：仅保护指令，数据内存（如栈、堆）仍明文暴露。
- **安全假设**：信任边界在CPU核内部，未防御物理攻击（如探针、侧信道），且时序/地址模式可能泄露粗粒度程序结构。
- **对比实验缺失**：未与SOFIA、CONFIDAENT、SCFP等方案在同一平台上进行定量比较，仅靠定性表格定位。

（完）

---
title: Auditable Session Admission for Cross-Silo Federated Learning
title_zh: 跨孤岛联邦学习中的可审计会话准入
authors: "Enzo Fenoglio, Philip Treleaven"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16559v1"
tags: ["query:sec-cloud-ai"]
score: 7.0
evidence: 跨孤岛联邦学习的会话准入与身份认证
tldr: 跨孤岛联邦学习面临跨组织会话操作准入的审计难题，现有集中式策略和临时检查易漂移且难审计。本文提出会话准入网关，通过会前签发签名能力令牌（含操作权限和角色约束）并绑定请求证明所有权，运行时无状态密码验证和权限匹配，拒绝重放和冒用。以MNIST为代理工作负载端到端验证成功，开源容器化实现包含可复现测试和证据日志。该网关实现边界可审计准入，降低审计复杂度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16559v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 600, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16559v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1049, \"height\": 715, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16559v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1853, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16559v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 840, \"height\": 226, \"label\": \"Table\"}]"
motivation: 跨孤岛联邦学习中跨组织会话操作准入缺少边界可审计机制，现有方法依赖集中策略或临时检查，随时间漂移且难以从边界证据审计。
method: 设计会话准入网关：会话建立时颁发签名能力令牌（含权限和角色约束），运行时令牌附带请求绑定证明所有权，网关进行无状态密码验证和权限匹配。
result: 以MNIST为代理工作负载完成端到端验证，开源容器化实现包含可复现测试和证据日志，证明方案可行。
conclusion: 该网关提供跨组织会话操作的边界可审计准入，降低审计复杂度，适用于多种跨孤岛FL场景。
---

## 摘要
跨孤岛联邦学习保持原始数据本地化，但在决定谁可以在跨组织边界上调用哪些会话范围操作时，部署常常停滞在一个实际瓶颈上，这些操作需在执行后仍保持可审计性。实践中，准入通过集中式策略服务、平台配置或临时检查实现，这些方法会随时间漂移，且难以从边界可见的证据中进行审计。我们提出了一种会话准入网关，该网关在边界处强制实施预批准的会话能力，并生成可验证的决策记录。在会话建立期间，参与组织批准角色和约束，并铸造签名的会话能力令牌，其中列举了针对给定会话ID允许的会话操作。运行时，每个请求携带该令牌和一个请求绑定的持有证明，从而通过请求绑定在网关处检测到重放和利用被盗令牌的冒充。准入简化为在准入网关处进行无状态的每请求密码学验证和能力匹配，而设置和编排仍在范围之外且由组织特定。我们在一个使用MNIST作为替代工作负载的跨孤岛FL工作流上端到端验证了该方法。概念验证是开源的、容器化的、作为代码配置，并包含可重现的测试和证据日志。

## Abstract
Cross-silo federated learning keeps raw data local, but deployments frequently stall on a practical bottleneck when deciding who may invoke which session-scoped operations across organizational boundaries, under constraints that remain auditable after execution. In practice, admission is implemented via centralized policy services, platform configuration, or ad hoc checks, which drift over time and are hard to audit from boundary-visible evidence. We present a session admission gateway that enforces pre-approved session capabilities at the boundary and emits verifiable decision records. During session setup, participating organizations approve roles and constraints and mint signed session capability tokens that enumerate permitted session operations for a given session_id. At runtime, each request carries the token and a request-bound proof-of-possession, making replay and impersonation with stolen tokens detectable at the gateway via request binding. Admission reduces to stateless per-request cryptographic verification and capability matching at an admission gateway, while setup and orchestration remain out of scope and organization-specific. We validate the approach end-to-end on a cross-silo FL workflow using MNIST as a surrogate workload. The proof-of-concept is open-source, containerized, provisioned as code, and includes reproducible tests and evidence logs.
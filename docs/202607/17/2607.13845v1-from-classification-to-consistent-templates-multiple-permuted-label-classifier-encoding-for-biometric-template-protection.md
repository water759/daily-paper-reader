---
title: "From Classification to Consistent Templates: Multiple Permuted-Label Classifier Encoding for Biometric Template Protection"
title_zh: 从分类到一致性模板：面向生物特征模板保护的多重置换标签分类器编码
authors: "Baogang Song, Zhongshu Zhao, Qianrong Zheng, Jianwen Xiang, Dongdong Zhao"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13845v1"
tags: ["query:sec-cloud-ai"]
score: 8.0
evidence: 使用加密哈希的生物特征模板保护
tldr: 针对生物特征模板保护中现有方法依赖相似性匹配、纠错码或预定义映射，存在结构泄露、辅助数据风险或模态依赖的问题，提出MPLCE方法。通过多分类器特定标签置换将可变样本映射到稳定身份输出，编码并哈希实现精确匹配。在四个面部和两个虹膜数据集上取得优异性能，安全分析支持不可逆、可撤销和不可链接。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13845v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1812, \"height\": 931, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13845v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1836, \"height\": 1211, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13845v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1855, \"height\": 950, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13845v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 931, \"height\": 554, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13845v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 934, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13845v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1867, \"height\": 602, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13845v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 935, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13845v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 934, \"height\": 529, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13845v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1704, \"height\": 339, \"label\": \"Table\"}]"
motivation: 现有方法依赖相似性匹配、纠错码或预定义映射，存在结构泄露、辅助数据风险或模态依赖。
method: 提出MPLCE，通过多分类器特定标签置换，将可变样本映射到稳定身份输出，编码并哈希实现精确匹配。
result: "在YTF上FAR=5.51e-5%时GAR=98.61%；CASIA-Iris-Lamp上FAR=0%时GAR=99.10%。"
conclusion: 实现了不可逆、可撤销和不可链接的模板保护，且跨模态适用。
---

## 摘要
生物特征模板保护（BTP）必须在容忍类内变异的同时保护存储的模板。现有方法依赖保护域相似性匹配、纠错或预定义模板映射，可能保留可利用的相似性结构、引入辅助数据风险、依赖人工目标或将保护耦合到特定模态。仅存储密码学哈希摘要可消除直接可比较的表示并隐藏哈希前模板，但基于哈希的精确匹配验证要求真实样本在哈希前生成相同的中间模板。身份分类天然适合这一要求，因为它将可变的生物特征样本映射到稳定且具有判别性的身份级输出。基于这一见解，我们提出多重置换标签分类器编码（MPLCE）。通过分类器特定的标签置换，MPLCE为每个身份在不同分类器分配不同标签。预测的标签被编码并拼接成中间模板，防止单个身份标签的重复编码，并在保持分类一致性的同时扩大有效候选空间。该模板通过特定应用的XOR字符串随机化并密码学哈希，无需纠错码或依赖生物特征的辅助数据即可实现精确匹配验证。使用模态特定分类器，MPLCE在不同模态间保持相同的模板生成和保护流程。在四个面部和两个虹膜数据集上，MPLCE取得了有竞争力的性能，包括在YTF上0.00% FAR下98.61%的GAR，以及在CASIA-Iris-Lamp上0.00% FAR下99.10%的GAR。安全分析和攻击评估支持其在威胁模型下的不可逆性、可撤销性和不可链接性。

## Abstract
Biometric template protection (BTP) must secure stored templates while tolerating intra-class variations. Existing methods rely on protected-domain similarity matching, error correction, or predefined-template mappings, potentially retaining exploitable similarity structures, introducing helper-data risks, depending on artificial targets, or coupling protection to specific modalities. Storing only cryptographic hash digests eliminates directly comparable representations and conceals pre-hash templates, but hash-based exact-match verification requires genuine samples to generate identical intermediate templates before hashing. Identity classification is naturally suited to this requirement because it maps variable biometric samples to stable and discriminative identity-level outputs. Based on this insight, we propose Multiple Permuted-Label Classifier Encoding (MPLCE). Through classifier-specific label permutations, MPLCE assigns each identity different labels across multiple classifiers. The predicted labels are encoded and concatenated to form an intermediate template, preventing repeated encodings of a single identity label and enlarging the effective candidate space while preserving classification consistency. The template is randomized with an application-specific XOR string and cryptographically hashed, enabling exact-match verification without error correction codes or biometric-dependent helper data. Using modality-specific classifiers, MPLCE retains the same template generation and protection procedure across modalities. On four face and two iris datasets, MPLCE achieves competitive performance, including a GAR of 98.61\% at a FAR of 5.51\(\times\)10\textsuperscript{-5}\% on YTF and a GAR of 99.10\% at a FAR of 0.00\% on CASIA-Iris-Lamp. Security analyses and attack evaluations support its irreversibility, revocability, and unlinkability under the threat model.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有生物特征模板保护（BTP）方法在容忍类内变异时存在多种局限性：依赖保护域相似性匹配（保留可利用的相似结构）、依靠纠错码或辅助数据（引入额外安全风险）、耦合特定模态设计（难以跨模态统一），且基于密码学哈希的精确匹配需要生成完全一致的中间模板，而生物特征样本天然存在类内差异，难以直接通过连续特征或模板实现。
- **整体含义**：论文提出利用身份分类的天然稳定性——分类器可将可变样本映射为离散、稳定的身份标签——来生成一致中间模板，从而在哈希前消除类内变异，实现安全、精确、模态无关的生物特征模板保护。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过多个**独立训练的、带标签置换的分类器**，将同一身份的样本映射为不同的预测标签序列，编码并拼接成中间模板，再经应用特定XOR随机化和密码学哈希生成保护模板，实现**精确匹配验证**（无相似性度量、无纠错码、无生物特征相关辅助数据）。
- **关键技术细节**：
  - **标签置换**：对大小为 \(N\) 的身份集生成 \(m\) 个双射置换 \(\pi_j: \mathcal{Y} \to \mathcal{Y}\)，每个置换只改变标签映射，不改变样本。
  - **多分类器训练**：对每个置换后的标签集 \(\mathcal{D}^{(j)} = \{(x_i, \pi_j(y_i))\}\) 独立训练分类器 \(f_j(\cdot; \theta_j)\)，使用交叉熵损失。
  - **中间模板生成**：对于输入样本 \(x\)，每个分类器预测标签 \(\hat{z}_j(x) = \arg\max_k [f_j(x)]_k\)，将每个标签编码为 \(\ell = \lceil \log_2 N \rceil\) 位二进制（如 \(\text{Enc}(k)\) 表示 \(k-1\) 的二进制），按固定顺序拼接成 \(L = m\ell\) 位的中间模板 \(T(x)\)。
  - **保护模板生成**：\(P(x) = h(T(x) \oplus I)\)，其中 \(I\) 是应用特定的随机 XOR 串（不作秘密），\(h\) 为 SHA3-512。
  - **验证**：仅当查询保护模板与存储保护模板完全相等时接受。
  - **候选空间**：有效中间模板数为 \(N^m\)，而非 \(2^L\)，枚举难度随 \(m\) 指数增长。

- **算法流程**（文字描述）：
  1. 对每个 \(j=1..m\)，生成随机置换 \(\pi_j\)，根据置换重标签训练集。
  2. 用重标签数据集训练分类器 \(f_j\)，得到有序分类器序列 \(\mathcal{F}\)。
  3. 注册时，对注册样本经所有 \(f_j\) 预测、编码、拼接得 \(T\)，与 \(I\) XOR 后哈希得 \(P\)，存储 \((ID, P, I)\)。
  4. 验证时，对查询样本同样处理得 \(P_q\)，与数据库对应 \(P\) 精确匹配。

### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：
  - **面部**：VGGFace2、FaceScrub、YouTube Faces (YTF)、Extended Yale B (Yale-B)
  - **虹膜**：CASIA-Iris-Lamp、CASIA-Iris-Thousand
- **场景与协议**：对每个数据集，固定 \(N\)（100/200/300）、\(m\)（8/12/16/20）、每个身份样本数 \(K\)（10~40）。YTF 和 Yale-B 遵循 BioDeepHash 的基准协议。
- **对比方法**：对比了多种现有 BTP 方法，包括：
  - 面部：Pandey et al. [27], Chen et al. [28], BioDeepHash [26] 等
  - 虹膜：Zhao et al. [50], Lee et al. [7], Othman et al. [52], Pinto et al. [53] 等
- **评价指标**：Genuine Acceptance Rate (GAR) 和 False Acceptance Rate (FAR)，均为百分数。由于精确匹配无阈值，直接报告 GAR@FAR（如 98.61%@5.51e-5%）。

### 4. 资源与算力

论文**未明确说明**使用的 GPU 型号、数量或单个实验的训练时长。仅提及训练细节：
- 面部使用 AdaFace（IR50/IR101，预训练于 MS1MV2）、FaceNet（预训练于 CASIA-WebFace），微调 20~30 个 epoch，batch size 64，Adam 优化器。
- 虹膜使用 ResNet18/34、DeepIrisNet，从头训练 30~60 个 epoch，batch size 32，联合使用交叉熵和中心损失。
- 所有模型在多个 \(N\) 和 \(m\) 设置下独立训练，但未报告总 GPU 小时数或具体算力配置。

### 5. 实验数量与充分性

- **实验数量**：论文进行了多维度实验：
  - 主性能实验：在 2 个面部数据集×3种骨干×3个 \(N\)×3个 \(m\) = 54 组 + 2 个虹膜数据集×3种骨干×3个 \(N\)×3个 \(m\) = 54 组，共 108 组基本结果（表 III）。
  - 设计分析：对比 Single、Multi w/o perm、MPLCE 在 2 个数据集上（表 IV）。
  - 参数敏感性：固定 \(N=200\)，变化 \(m\)（4 个值）和 \(K\)（3 个值），2 个数据集（表 V）。
  - 基准对比：在 YTF、Yale-B、Lamp、Thousand 上与 8 种已有方法比较（表 VI）。
  - 安全分析：候选空间理论计算（表 I）、距离相关性（表 II）、可链接性攻击评估（图 3，6 个子图）。
  - 实验轮次：默认 3 次独立运行取平均。
- **充分性与公平性**：
  - **优点**：涵盖多模态、多骨干网络、不同身份数和分类器数，消融实验清晰验证了置换和多分类器的作用，Benchmark 对比使用统一协议（BioDeepHash）及公开报告结果。
  - **客观性**：随机生成置换并共享前缀，减少随机性波动；报告了具体 GAR/FAR 数值，没有选择性展示。
  - **潜在不足**：部分对比方法的结果来自原论文（非复现），可能存在协议差异（如 Yale-B 上 BioDeepHash 报告更高 GAR），论文已指出差异应考虑协议不同。此外，对手工设计的 CB 方法（如 Bloom filter）的对比不够充分（表 VI 仅包含 NN 类方法）。

### 6. 论文的主要结论与发现

- MPLCE 在多个面部/虹膜数据集上实现了**高 GAR（≥88%）与极低 FAR（常 <0.001%）**，尤其在 CASIA-Iris-Lamp 上达到 99.5% GAR @ 0% FAR。
- **标签置换** 与 **多分类器** 相互补充：多分类器降低 FAR，置换扩大候选空间（从 \(N\) 到 \(N^m\)）。
- **安全性**：在完全泄露模型下（攻击者知道系统参数、数据库记录），理论分析和实验表明 MPLCE 满足不可逆性、可撤销性、不可链接性，候选空间足够大（\(N^m\)）防止暴力搜索，保护模板间无距离相关性，跨应用记录无法关联。
- **模态无关性**：方法适用于面部与虹膜，保护流程一致，仅需更换模态专用分类器。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 首次将**身份分类**与**标签置换**结合生成中间模板，巧妙利用分类的稳定性解决哈希前一致性难题，无需纠错码或辅助数据。
  - 通过多分类器扩大候选空间的同时，仍保持分类级一致性，避免单标签模板空间过小。
  - 保护环节纯依赖密码学哈希和 XOR，不保留任何可比对的结构信息，彻底消除相似性泄露风险。
- **实验设计**：
  - 消融实验（Single vs. Multi w/o perm vs. MPLCE）清晰证明了置换与多分类器的各自贡献。
  - 参数敏感性分析（\(m\) 和 \(K\)）揭示在候选空间扩大与匹配一致性之间的 trade-off。
  - 安全评估全面：包括理论候选空间、距离相关性、ARM 攻击，使用 ISO/IEC 标准度量，结果支持声明。
- **跨模态验证**：同时评估面部和虹膜，证明方法通用性。

### 8. 不足与局限

- **实验覆盖**：
  - 未在其他模态（如指纹、掌纹）上验证，不一定保证通用性。
  - 仅包含 NN 类对比方法，未与传统 CB（如 Bloom filter, IoM）或 BCS（如 fuzzy vault）在统一协议下比较。
  - 训练资源（GPU 型号、数量、时间）未报告，不利于复现和能耗评估。
- **偏差风险**：
  - 面部数据集主要为西方人脸（VGGFace2, FaceScrub, YTF），可能存在种族/年龄偏差；虹膜数据集为 CASIA 系列，可能缺乏多样性。
  - 所有实验在固定身份集（无增量注册）下进行，未考虑实际部署中动态添加用户的场景。
- **应用限制**：
  - 需要对每个用户注册时选择一条稳定的中间模板（本文采用多数投票），但若注册样本异质性高，投票结果可能不稳定。
  - 模型训练阶段需要已注册用户的训练样本（即标签需已知），不适合无需预注册的开放集场景。
  - 一旦分类器参数被攻击者获取（威胁模型中已假设），若分类器输出可被反向推演，可能削弱安全性（论文承认模型级攻击不在保护范围内）。论文强调仅保护存储记录，但实际系统需要同时防御模型泄露。

（完）

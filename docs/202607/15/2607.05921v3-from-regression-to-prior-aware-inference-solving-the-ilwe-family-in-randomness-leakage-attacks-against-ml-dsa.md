---
title: "From Regression to Prior-Aware Inference: Solving the ILWE Family in Randomness Leakage Attacks against ML-DSA"
title_zh: 从回归到先验感知推理：解决针对ML-DSA随机性泄漏攻击中的ILWE问题族
authors: "Peiheng Zhang, Yuejun Liu, Wei Cheng, Muye Li, Honglin Shao, Yongbin Zhou"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05921v3"
tags: ["query:sec-cloud-ai"]
score: 9.0
evidence: 针对ML-DSA签名方案随机性泄露的密钥恢复攻击
tldr: ML-DSA签名随机数泄漏可转化为ILWE系列问题，现有研究侧重构造实例而忽视求解器影响。本文提出统一框架评估三类求解器在普通ILWE、FS-ILWE和CILWE上的性能。实验发现先验感知离散推理相比普通最小二乘和置信传播，在FS-ILWE上将所需关系数降低10.5至73.9倍。系统评估为ML-DSA随机泄漏攻击提供新基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05921v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1688, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1513, \"height\": 1226, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v3/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1697, \"height\": 747, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v3/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 929, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v3/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1702, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05921v3/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 931, \"height\": 1214, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05921v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05921v3/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 691, \"height\": 160, \"label\": \"Table\"}]"
motivation: 现有随机泄漏攻击忽略求解器对密钥恢复效率的关键影响，亟需统一评估框架。
method: 构建涵盖普通ILWE、FS-ILWE和CILWE的实例，采用OLS、BP和先验感知离散推理三类求解器对比。
result: 在FS-ILWE上先验感知离散推理减少10.5到73.9倍关系数，显著提升恢复效率。
conclusion: 求解器选择至关重要，为ML-DSA随机泄漏攻击提供新基准与优化方向。
---

## 摘要
ML-DSA是NIST标准化的一种代表性格基签名方案。它依赖于签名随机性和拒绝采样来确保发布的签名与私钥统计独立。然而，实际实现可能会泄漏关于该随机性的部分信息，这种泄漏可以将公开签名转化为ILWE类型的问题，导致私钥泄露风险。此类随机性泄漏攻击可被表述为一个两阶段的密钥恢复过程：首先将泄漏的部分信息和公开签名转化为一个ILWE问题族实例，然后应用恢复求解器来恢复私钥。现有工作主要集中在第一阶段，即在不同泄漏模型下构建此类实例。相比之下，求解器在后续实例求解阶段的作用仍未得到充分探索，现有攻击通常依赖特定于模型的临时求解器。为解决这一差距，我们提出了一个统一框架，系统性地评估泄漏衍生的ILWE问题族实例上的不同恢复求解器。该框架涵盖了三种ILWE实例，包括普通ILWE、Fiat-Shamir ILWE (FS-ILWE) 和隐蔽ILWE (CILWE)，适用于不同场景。在我们的框架内，我们探索了三类求解器。实验表明，求解器对私钥恢复效率有显著影响。特别是在FS-ILWE上，与基线相比，先验感知离散推理将信息关系的数量减少了一到两个数量级：与OLS相比，BP在无噪声设置中减少了15.4倍至64.9倍，在有噪声设置中减少了10.5倍至73.9倍。总体而言，这项工作系统评估了随机性泄漏攻击中的不同求解器，并为未来对ML-DSA的分析提供了新基准。

## Abstract
ML-DSA is a representative lattice-based signature scheme standardized by NIST. It relies on signing randomness and rejection sampling to ensure that released signatures are statistically independent of the secret key. Practical implementations, however, may leak partial information about this randomness, and such leakage can transform public signatures into ILWE-type problems, resulting in secret key disclosure risks.   Such randomness leakage attack can be formulated as a two-stage key-recovery procedure, in which leaked partial information and public signatures are first transformed into an ILWE-family instance, and then a recovery solver is applied to recover the secret key. Existing work has mainly focused on the first stage by constructing such instances under different leakage models. By contrast, the role of solver in the subsequent instance-solving stage remains under-explored, and existing attacks often rely on ad-hoc model-specific solvers. To address this gap, we propose a unified framework to systematically evaluate different recovery solvers on leakage-derived ILWE-family instances. The framework covers three ILWE instances, including the ordinary ILWE, Fiat-Shamir ILWE (FS-ILWE) and Concealed ILWE (CILWE) under different scenarios.   Within our framework, we explore three classes of solvers. Our experiments show that the solver has a significant impact on the secret-key recovery efficiency. In particular, on FS-ILWE, prior-aware discrete-inference reduces the number of informative relations by one to two orders of magnitude compared to the baselines: Compared with OLS, BP constitutes a reduction by a factor of 15.4x-64.9x in noise-free settings, and by a factor of 10.5x-73.9x in noisy settings. Overall, this work provides a systematic evaluation on different solvers in randomness leakage attacks, and presents new benchmarks for future analysis on ML-DSA.

---

## 论文详细总结（自动生成）

# 论文总结：From Regression to Prior-Aware Inference: Solving the ILWE Family in Randomness Leakage Attacks against ML-DSA

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：ML-DSA（NIST标准化的格基数字签名）在签名过程中依赖随机性向量 \(y\) 和拒绝采样来保证签名与私钥 \(s_1\) 统计独立。然而，实际实现可能通过侧信道（如功耗、微架构）泄漏 \(y\) 的部分信息，导致公开签名可被转化为整数学习误差（ILWE）问题，进而恢复私钥。
- **现有不足**：已有攻击主要关注**第一阶段**——如何从不同泄漏模型（如随机性比特泄漏、零系数泄漏）构造ILWE族实例（FS-ILWE、CILWE）。但**第二阶段**——如何高效求解这些ILWE实例——缺乏系统研究，现有求解器多为特定模型临时设计，未在统一框架下比较。
- **研究目标**：提出统一框架，系统评估不同求解器在泄漏衍生的ILWE实例上的密钥恢复效率，揭示求解器选择对攻击成本的决定性影响。

## 2. 方法论
### 核心思想
- 将随机性泄漏攻击抽象为两阶段：① 从泄漏信息和公开签名构造ILWE族实例；② 应用求解器恢复私钥。本文聚焦第二阶段，将OILWE、FS-ILWE、CILWE统一为形如 \(b = As + e\) 的ILWE问题，并比较三类求解器。
### 关键技术细节
- **三类求解器**：
  - **最小二乘回归求解器**：OLS、SVD、QR、GD-MLE（基于 \(\ell_2\) 损失）。输出实值估计，再通过坐标舍入（coordinate-wise rounding）得到整数私钥。适用于误差中心且独立时。
  - **鲁棒回归求解器**：Huber回归、Cauchy回归（通过IRLS迭代实现）。通过鲁棒损失函数降低异常样本影响，但仍需坐标舍入。适用于含离群点或隐藏样本的场景。
  - **先验感知离散推理求解器**：置信传播（BP）、目标诱导贪婪搜索、有界误差爬山。直接在小整数秘密空间 \(S \subseteq [-\eta, \eta]^n\) 上进行推理或搜索，利用稀疏挑战结构、秘密先验和误差模型。
- **BP的具体流程**：
  - 每个关系对应一个因子 \(\phi_i(x_{N(i)}) = \chi_e(b_i - \sum a_{i,j}x_j)\)。
  - 通过动态规划实现因子到变量消息更新，避免枚举所有组合。
  - T次迭代后取最大后验估计。
- **FS-ILWE的构造**：利用泄漏比特 \(y_{i,j}\) 结合 \(z_i, c_i\) 进行移位校正，得到边界误差 \(\beta_{\text{eff}}\)（\(\min\{\beta, 2^{j-1}\}\)）的有界关系。
- **CILWE的构造**：通过模板攻击预测随机性零系数，形成零误差样本与隐藏样本（均匀大误差）的混合，使用混合似然因子 \(\phi_i = (1-p_{\text{con}})D_0 + p_{\text{con}} D_{\text{con}}\)。

## 3. 实验设计
### 数据集/场景
- **OILWE（普通ILWE基线）**：人工生成数据，\(n \in \{256,512\}\)，秘密稀疏（15% ±1, 15% ±2, 其余0），样本和误差均匀分布，误差宽度 \(\alpha_e \in \{78,120,196\}\)，\(\alpha_a \in \{1,5\}\)。
- **FS-ILWE（无噪声）**：基于ML-DSA三个安全级别（ML-DSA-44, -65, -87），泄漏位置 \(j=4,5,6,7\)（-44/-87）或 \(j=5,6,7,8\)（-65）。利用公开挑战-响应对和泄漏比特构造。
- **FS-ILWE（有噪声）**：同一场景下引入比特翻转概率 \(p_{\text{flip}} \in [0,0.45]\)。
- **CILWE**：混合生成，隐藏率 \(p_{\text{con}} \in [0,0.9]\)，零误差样本 vs. 隐藏样本（均匀大误差）。

### 基准（Baselines）
- **最小二乘类**：OLS、SVD、QR、GD-MLE（实现自[12][13]）
- **鲁棒回归类**：Huber、Cauchy（实现自[6]）
- **先验感知离散推理**：BP（实现自[15]）、目标诱导贪婪（自行实现，使用 \(\ell_2\) 损失）、有界误差爬山（实现自[10]）
- 此外，Greedy-\(\ell_2\) 也被归入先验感知类但使用 \(\ell_2\) 分数。

### 对比方法
- 在同一生成关系序列上测试所有求解器，通过倍数扫描和二分查找确定最小所需样本/关系数。
- 每组参数5次随机种子取平均。

## 4. 资源与算力
- **论文未明确说明使用的GPU型号、数量、训练时长等算力信息**。所有实验基于CPU实现（BP、IRLS等迭代算法），未提及分布式或大规模计算资源。仅指出某些场景（如OLS在超大规模下矩阵求逆成本高）但未量化。

## 5. 实验数量与充分性
- **实验组数**：覆盖了3种ILWE变体、3个安全级别、多个泄漏位置/噪声率/隐藏率，以及7-8种求解器，总计数十种组合，每组5次重复验证。
- **充分性**：实验覆盖了典型参数范围（低高泄漏位置、低高噪声、低高隐藏率），并包含消融（如对比BP与OLS、鲁棒回归在不同场景的表现）。在无噪声FS-ILWE中扩展了以往工作未覆盖的低位泄漏场景。
- **公平性**：所有求解器使用相同的关系序列，最小样本通过统一二分查找确定，客观比较恢复能力。但注意：各求解器实现可能受代码效率影响（如BP动态规划特定优化），存在偶然偏差但文中已尽量使用同行标准实现。

## 6. 主要结论与发现
- **求解器选择至关重要**：在大多数场景下，先验感知离散推理（尤其是BP）显著优于最小二乘和鲁棒回归。
  - **FS-ILWE无噪声**：BP比OLS减少所需关系数 **15.4–64.9倍**；比爬山减少最多 **7.6倍**。
  - **FS-ILWE有噪声**：BP比OLS减少 **10.5–73.9倍**，且能在更高翻转率下恢复（OLS在 \(p_{\text{flip}}>0.43\) 时失效，BP可至0.45）。
  - **CILWE**：在隐藏率 \(p_{\text{con}}<0.9\) 时BP最优；在 \(p_{\text{con}} \geq 0.9\) 时Cauchy回归更鲁棒。
- **鲁棒回归的局限性**：鲁棒回归仍依赖坐标舍入，在纯误差模型中无优势，仅在有严重离群点（隐藏样本）时有用。
- **先验感知的优势来源**：利用秘密离散结构、稀疏挑战、误差边界直接推理，避免舍入误差，样本需求大幅降低。

## 7. 优点
- **统一框架**：首次将OILWE、FS-ILWE、CILWE纳入同一ILWE族进行求解器横向对比，填补了第二阶段求解器系统评估的空白。
- **全面的求解器覆盖**：从连续回归到离散推理的三个类别，实现了从经典到先进方法的完整比较。
- **实用导向的实验设计**：覆盖多个ML-DSA参数、泄漏位置、噪声模型和隐蔽率，实验充分，结果有统计意义。
- **揭示了关键规律**：BP在多数场景下极大降低样本需求，为实际侧信道攻击提供效率提升方向；同时指出鲁棒回归在极端隐蔽率下的价值。

## 8. 不足与局限
- **未评估计算开销**：虽然BP所需样本少，但每次消息传递迭代可能比OLS更耗时。论文未报告时间或内存消耗，不能直接说明在总攻击时间上BP更优。
- **未考虑实际泄漏的完整性**：论文假设已从泄漏中提取出干净的ILWE关系，但实际侧信道获取（如波形处理、模板分类）可能引入误差或增加成本，这些未纳入统一框架。
- **CILWE中场景覆盖有限**：BP仅在 \(p_{\text{con}}<0.9\) 时最优，未探索 \(p_{\text{con}}>0.9\) 的极限情况；也未测试混合误差分布（如部分隐藏样本有小误差）的影响。
- **依赖理想化假设**：BP需已知误差分布和隐藏率；爬山需人工设定有效边界。实际攻击中这些参数可能未知或需估计，导致鲁棒性下降。
- **未讨论与现有防护措施的相互作用**：如掩码（masking）如何影响泄漏模型的实际情况，求解器是否仍有效。

（完）

---
title: "LibFHE: A Numba-Based CUDA-Python Library for Non-RNS CKKS-BGV Fully Homomorphic Encryption on GPUs"
title_zh: "LibFHE: 基于Numba的CUDA-Python库，用于GPU上的非RNS CKKS-BGV全同态加密"
authors: John Chiang
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05920v1"
tags: ["query:sec-cloud-ai"]
score: 9.0
evidence: GPU加速的全同态加密库
tldr: 针对当前FHE GPU加速研究多集中于RNS-CKKS变体，而原始非RNS CKKS-BGV框架缺乏高效GPU实现的问题，提出LibFHE库。它基于Numba和CUDA-Python，实现对非RNS CKKS-BGV的GPU优化，性能可媲美高度优化的C++库。同时显著降低了实现复杂度，提升了可编程性。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05920v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1347, \"height\": 618, \"label\": \"Table\"}]"
motivation: 多数GPU加速FHE库采用RNS-CKKS，而原始非RNS框架的GPU实现尚未充分探索，且CUDA-Python在保持高性能的同时能简化开发。
method: 基于Numba的CUDA-Python库，重新实现非RNS CKKS-BGV框架，并针对GPU架构进行优化，包括算法级和内存访问优化。
result: 优化的CUDA-Python实现达到与高度优化C++ FHE库相当的性能，同时代码量更少、可读性更强。
conclusion: 证实了CUDA-Python能够兼顾高性能和易用性，为非RNS FHE的GPU加速提供了可行方案。
---

## 摘要
自第四代FHE框架CKKS提出以来已过去十年；然而，至今仍没有指向第五代继任者的迹象；近年来，大量研究探索了GPU加速以提高同态计算的效率。本文提出LibFHE，一个高性能GPU加速框架，它利用CUDA-Python绑定，在同态工作负载中实现了高级可编程性和裸金属GPU性能。大多数现有实现采用RNS-CKKS变体，而本工作有意重新审视原始（非RNS）CKKS-BGV框架，并开发了基于GPU的实现及相应的优化。实验结果表明，优化的CUDA-Python实现可以达到与高度优化的C++ FHE库相当的性能，同时显著降低实现复杂度并提高可编程性。

## Abstract
It has been a decade since the fourth-generation FHE framework, CKKS, was proposed; yet, there is still no indicator pointing toward a fifth-generation successor; and in recent years, numerous studies have explored GPU acceleration to improve the efficiency of homomorphic computations. In this paper, we propose LibFHE, a high-performance GPU-accelerated framework that features CUDA-Python bindings to achieve both high-level programmability and bare-metal GPU performance for homomorphic workloads. A large majority of state-of-the-art implementations adopt the RNS-CKKS variant. In contrast, this work deliberately revisits the original (non-RNS) CKKS-BGV framework, and develops a GPU-based implementation along with corresponding optimizations. Experimental results demonstrate that optimized CUDA-Python implementations can achieve performance comparable to highly optimized C++ FHE libraries, while significantly reducing implementation complexity and improving programmability.

---

## 论文详细总结（自动生成）

# LibFHE 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前 GPU 加速全同态加密（FHE）的研究几乎全部集中在基于 RNS（Residue Number System）的 CKKS 变体上，而原始的（非 RNS）CKKS-BGV 框架因被认为难以高效映射到 GPU 而未被充分探索。同时，现有 FHE 加速库多以 C++ 编写，开发效率低，与 Python 主导的 AI/ML 生态集成困难。
- **整体含义**：论文旨在回答“Python 能否在 FHE 场景下达到 C++ 级 GPU 性能”这一问题。通过利用 NVIDIA 最新推出的原生 CUDA-Python 生态（Numba + cuda-python），设计并实现首个全 Python 编写的非 RNS CKKS-BGV GPU 加速库 LibFHE，证明高可编程性语言同样能实现接近裸金属硬件的性能，从而降低 FHE 的应用门槛。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：
  - 放弃主流 RNS-CKKS 的“垂直 RNS 轴”，以保留原始 CKKS 的动态精度控制和编程灵活性；同时保留“水平 NTT 轴”以实现高吞吐的 SIMD 槽运算。
  - 采用混合表示：默认使用 32 位 limb 基的大整数（radix）表示，仅在多项式乘法时临时转换为非 RNS（30–31 位素数）的有限域，通过 NTT 加速。
- **关键技术细节**：
  - **多项式乘法流水线**：Limb → RNS 分解 → 负循环 Twiddle 预处理 → 前向 NTT → 点乘 → 逆 NTT → 逆 Twiddle → RNS → 混合基（Garner 算法）CRT 重建 → Limb。
  - **NTT 优化**：采用矩阵四步变换（1D→2D 重塑、行蝴蝶计算、矩阵转置、继续蝴蝶计算）改善 GPU 访存局部性；实现两遍 NTT 分解（列变换 + 行变换）配合共享内存 tile 融合。
  - **Karatsuba 多项式乘法**：用于减少密文乘法中基多项式乘法的次数（从 4 次减为 3 次）。
  - **预计算**：NTT 所需的原根、twiddle 因子、CRT/Garner 常数等全部在 CPU 侧预计算并传输至 GPU，消除运行时重计算。
  - **CipherTensor 数据结构**：将多个同参数密文批量打包，支持 pairwise 和 broadcast 两种执行模式，提高 GPU 占用率。

## 3. 实验设计：数据集/场景、Benchmark、对比方法
- **实验场景**：典型 CKKS-BGV 同态操作，包括加密/解密、自举、加/减、乘、旋转、共轭、常数操作、重新缩放等。
- **数据集**：未使用真实外部数据集；采用随机生成的明文向量，每个 CipherTensor 包含 4 个密文。
- **基准参数**：多项式次数 \(N=2^{16}\)，密文模数位数 \( \log Q = 1024 \)，缩放因子位数 \( \log p = 60 \)（自举时 \( \log p=30, \log q=40 \)），槽位数参数 \( \log s = 2 \)。
- **对比方法**：论文在摘要中声称性能与高度优化的 C++ FHE 库（如 SEAL、OpenFHE）相当，但在实验部分**未提供任何与现有库的直接性能对比数据**，仅列出了 LibFHE 自身各操作的绝对延迟。缺乏与其他 GPU 库（如 Cheddar、TensorFHE）或 CPU 库的定量比较。

## 4. 资源与算力
- **GPU 型号**：NVIDIA Tesla T4（Turing 架构，15 GB 显存）。
- **环境**：Google Colab，CUDA Version 13.0，Driver Version 580.82.07。
- **算力信息**：仅使用单 GPU，未提及多 GPU 或多节点分布式试验。论文未提供训练时长（注：此任务为推理/计算密集型，无需训练）。自举操作耗时约 232 秒/4 密文。

## 5. 实验数量与充分性
- **实验数量**：共报告了 14 种不同同态操作的延迟（表 1），每个操作一个数据点，无消融实验（如不同 cnum、不同 logN、不同模数链长度等）。
- **充分性与公平性**：
  - **充分性不足**：缺乏与现有主流库的对比实验，无法真正证明“性能媲美 C++”。缺乏对不同参数配置下的性能变化分析。
  - **客观性尚可**：测试在无干扰的 Colab 空闲 GPU 上进行，并经过 JIT 预热消除编译开销。
  - **唯一对比**：仅内部比较了不同操作的耗时，未提供 baseline 或 SOTA 对比数据。

## 6. 论文的主要结论与发现
- **主要结论**：
  - 通过精心设计的 GPU 内存布局和混合表示策略，非 RNS 的 CKKS-BGV 可以高效映射到现代 GPU 架构上。
  - 基于 Python 和 Numba/CUDA-Python 的实现，在获得接近 C++ 级性能的同时，显著降低了实现复杂度并提升了可编程性。
  - CipherTensor 批量执行模型能有效摊销内核启动和内存管理开销。
- **重要发现**：自举在同态操作中占据绝对主导的计算成本（约 58 秒/密文），多项式乘法、旋转等操作次之，而加、减、常数操作极快。

## 7. 优点
- **方法创新**：首次以全 Python（Numba + CUDA-Python）实现端到端非 RNS FHE 库，填补了该方向的空白。
- **可编程性**：原生集成 PyTorch/NumPy 生态，降低 AI 开发者使用 FHE 的门槛。
- **混合表示策略**：巧妙平衡了 radix 表示（灵活性）与 RNS 表示（并行性）在 GPU 上的优势，通过原位转换减少数据移动。
- **优化全面**：预计算、矩阵四步变换、Karatsuba、batch 执行等优化组合得当。

## 8. 不足与局限
- **实验不足**：
  - 未与任何现有 GPU FHE 库（如 Cheddar、TensorFHE、HEonGPU）或 CPU 库进行直接性能对比，导致“性能媲美 C++”的断言缺乏数据支撑。
  - 仅测试单一 GPU（T4）和固定参数集，缺少对不同安全参数、不同多项式维度、不同批次大小的扩展性分析。
  - 无消融实验，难以量化各优化技巧（如混合表示、Karatsuba、矩阵四步变换等）的单独贡献。
- **局限性**：
  - 当前实现未集成 Barrett 约减、Montgomery 约减、内核融合（kernel fusion）、CUDA 流、多 GPU 等高级优化，性能可能仍有很大提升空间。
  - 非 RNS 方案本身在安全性和性能上是否有明确优势存疑，论文未论证为何选择非 RNS 而非优化后的 32-bit RNS 变体。
  - 仅支持 \(N=2^{16}\) 且模数 1024 位的固定组合，适用场景有限。
  - 缺乏对实际应用（如加密神经网络推理）的端到端测试。

（完）

Title: Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes

URL Source: https://arxiv.org/pdf/2607.03392v1

Published Time: Tue, 07 Jul 2026 01:05:42 GMT

Number of Pages: 13

Markdown Content:
# Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

Gergely Flamich 1 ¨Oyk ¨ u Sıla G ¨ uner 1 Yanxiao Liu 1 Deniz G ¨ und ¨ uz 1

# Abstract 

The ever-increasing collection of personal data has created mounting pressure to develop tech-nologies that protect sensitive aspects of individ-ual identity. Differential privacy (DP) provides a principled framework with strong formal guaran-tees and has already achieved practical success. However, releasing high-dimensional data, such as images, has remained elusive: releasing un-compressed privatized data requires significant storage. At the same time, no effective data com-pression scheme exists that can compress high-resolution data with privacy guarantees. We address this challenge with DP-DiPP , a com-pression pipeline that combines stochastic codes with diffusion models. DP-DiPP is highly flexible: the practitioner has direct control over the com-pression rate-privacy-utility tradeoff. As the the-oretical backbone, we extend the Poisson private representation (PPR) of (Liu et al., 2024) to en-code the outputs of privacy mechanisms. We then combine it with DiffC, a diffusion-based lossy data compression method, to obtain a differen-tially private image compressor. Our experiments on privatized image classification on CIFAR-10 demonstrate that DP-DiPP significantly outper-forms the baseline, achieving a 10-30 times better compression while retaining comparable privacy guarantees and utility. 

# 1. Introduction 

Over the past decades, the rapid growth in data collection and its use in downstream systems such as machine learn-ing has produced undeniable benefits. Yet, this has also raised serious concerns about the identifiability of sensitive information and individual privacy. Therefore, developing frameworks and algorithms that protect privacy is essential  

> 1Imperial College London, London, UK. Correspondence to: Gergely Flamich <g.flamich@imperial.ac.uk >.
> Preprint. July 7, 2026.

to ensure user trust. In this paper, we adopt differential privacy (DP) as the privacy framework, a stronger variant of DP (DP; Dwork et al., 2006; Dwork & Roth, 2014). DP has already found practical success, such as for collecting user data (Erlingsson et al., 2014), for releasing privatized tabular data, like census data (United States Census Bureau, 2021), and for preventing generative models from memorizing and revealing their training sets (Liu et al., 2023). Local DP (LDP) protects individuals’ data by requiring that a system’s output reveal only limited information about its input. In practice, this idea translates to randomizing the sensitive data X before revealing it to an untrusted party, according to a privacy mechanism . For example, if X repre-sents location data, such as GPS coordinates (Andr ´es et al., 2013), one possible privacy mechanism is to add appropri-ately calibrated noise η to the coordinates to prevent precise localization, that is, Y = X + η. Of course, we still wish to use the privatized data for some purpose. Hence, a natural tradeoff arises between the privatized data’s utility for its downstream task and its privacy guarantee. However, despite its success for tabular data, scaling DP to high-dimensional data such as images, audio, or video remains challenging. This difficulty stems from several fac-tors. First, directly releasing privatized high-dimensional data can incur substantial storage and communication costs. Second, the noise required to provide meaningful privacy guarantees can severely degrade the data’s utility in high-dimensional domains. To the best of our knowledge, prior work has not systematically studied high-resolution priva-tized data compression, where the goal is to jointly preserve privacy, reduce storage cost, and retain downstream utility. This is the challenge we tackle in this paper. The theoretical foundation of our work is in the emerging field of data compression with stochastic codes (Flamich, 2024; Li et al., 2024; Flamich & G ¨und ¨uz, 2026). In practice, stochastic codes serve as a randomized lossy compression mechanism : for example, consider an input X (modeled as a random variable) and a perturbed, lossy version Y = X + η,where η is independent of X. Then, for a given X, stochas-tic codes produce a bitstring S that encodes one sample of 

Y | X, such that the expected length of S is as small as pos-sible. By utilizing stochastic codes, the generated sample 1

> arXiv:2607.03392v1 [cs.CR] 3 Jul 2026 Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes

exactly follows the conditional distribution. This is in sharp contrast to traditional lossy compression, where we first con-struct a quantizer and then attempt to determine the quan-tizer’s error distribution. This flexibility of stochastic codes naturally suggests their application to compressing the out-put of privacy mechanisms, where the exactness of sample distribution preserves other desirable statistical properties of the original privacy mechanism, such as unbiasedness and Gaussianity. Unfortunately, as we explain in Section 2.2, simply encoding the outputs of privacy mechanisms with stochastic codes can lose the privacy guarantees of the origi-nal mechanism; hence, we need to design special-purpose codes that preserve the guarantees. As a solution, we use the Poisson private representation (PPR; Liu et al., 2024) as a starting point for our work, a stochastic code that does have LDP guarantees. Concretely, we develop an approximate version (called “step-limited” in this paper) of PPR that encodes any pure ϵ-LDP mech-anism, ensuring that its output is at least 2αϵ -LDP, where 

α > 1 is a tunable parameter that interpolates between the code’s privacy and the expected codelength. Unfortunately, applying PPR to complex data is challenging due to its generally very slow runtime, scaling super-exponentially with the information content. To circumvent this issue, we combine our extended PPR code with DiffC, a variable-rate, diffusion-based data compression framework (Theis et al., 2022). DiffC progressively encodes each denoising step of a Gaussian diffusion model, thereby decomposing the compression problem into smaller, manageable steps. However, additive Gaussian noise never yields a pure ϵ-LDP mechanism. Hence, we approximate the Gaussian denoising targets at each step with moment-matched Laplace distribu-tions, which do provide pure ϵ-LDP guarantees, allowing us to apply our approximate PPR code. Somewhat surprisingly, we empirically find that this approximation does not degrade the denoiser’s performance, while significantly strengthen-ing its formal privacy guarantee. Thus, we call our method 

Differentially Private DiffC with Poisson Processes (DP-DiPP, pronounced dippy-dip ). Put succinctly, DP-DiPP jointly compresses and privatizes the data, rather than privatizing the data first and then com-pressing it losslessly. Thus, to validate our method, we conduct experiments on privatized image compression and classification on CIFAR-10 and compare it with the naive method. We find that DP-DiPP performs significantly bet-ter, consistently reducing bitrate by a factor of 10-30 while maintaining a comparable pure LDP guarantee and utility (as measured by classification accuracy). In summary, our main contributions are as follows: • In Section 3, we extend the Poisson private represen-tation (PPR) of Liu et al. (2024) to approximate sam-pling, and analyze its output sample quality and its DP guarantees. Notably, we show that it yields a 2αϵ -LDP certificate when used to encode any pure ϵ-LDP mech-anism, where α ≥ 1 is a tunable parameter that trades off privacy and approximation quality. • In Section 4, we modify DiffC, a variable-rate, Gaus-sian diffusion-based lossy data compression method, by replacing the Gaussian targets in the denoising pro-cess with Laplace distributions, which turns the reverse process into a pure ϵ-LDP mechanism. Surprisingly, we show that this approximation does not significantly degrade the denoiser’s performance. • In Section 5, we apply our approximate PPR code to the modified DiffC process, yielding our proposed method, DP-DiPP , and validate its performance on a privatized image compression task on the CIFAR-10 dataset. Our results show that DP-DiPP consistently reduces bitrate by a factor of 10-30 while maintain-ing a comparable pure LDP guarantee and utility (as measured by classification accuracy). 

1.1. Notation 

We denote the binary logarithm as log . For a finite set A,we denote the number of its elements as |A|, for a string 

s ∈ { 0, 1}∗, we denote its length by |s|. 1[·] denotes the indicator function. For a real-valued function f , we denote its supremum norm as ∥f ∥∞ = sup x|f (x)|. For two ran-dom variables X and Y and a probability distribution P , we write X ∼ P to mean that X has distribution P , and write 

X ∼ Y to mean that the variables are equal in distribution. We denote uniform distributions over the interval (a, b ) as 

Unif( a, b ), exponential distributions with rate λ as Exp( λ)

and normal distributions with mean μ and variance σ2 as 

N (μ, σ 2). Furthermore, we write X ⊥ Y to denote the independence of X and Y . For two probability distributions 

Q and P , we denote the relative entropy/Kullback-Leibler divergence of Q from P as DKL [Q ∥ P ] (in bits) and their total variation distance as DTV [Q ∥ P ]. For random vari-ables X, Y , we denote their mutual information as I[X ; Y ]

(in bits). Finally, O(·) is the standard big O notation. 

# 2. Background 

2.1. Privacy Mechanisms and Differential Privacy 

Differential privacy (DP) provides formal certificates that quantify how much information a system reveals about its input (Dwork et al., 2006; Dwork & Roth, 2014). In this paper, we concern ourselves with its strict variant, local DP (LDP): we take some sensitive input x and randomize it using a privacy mechanism M (x) to reduce the information leaked by x. However, not any randomization will do: in 

ε-LDP we choose a privacy parameter ε ≥ 0 and require the mechanism M to satisfy, for any two inputs x, x ′ and 2Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

any measurable event S,

P[M (x) ∈ S] ≤ eεP[M (x′) ∈ S].

It can be understood as ensuring a certain level of difficulty in distinguishing whether the output of M was generated from input x or input x′, for example, when using a likeli-hood ratio test as the hypothesis test. Naturally, in all cases, we wish to use the data for some purpose; otherwise, we might as well replace it with pure noise to achieve perfect privacy. Thus, we measure the use-fulness of the released data through some utility function 

u, and we are interested in maximizing the expected util-ity E[u(M (X))] under a given privacy level ϵ. Sadly, in virtually all cases of practical interest, maintaining privacy comes at the cost of utility; hence, we seek to characterize the system’s utility-privacy tradeoff (Chen et al., 2020). 

2.2. Compressing Differential Privacy Mechanisms 

When the sensitive data is scalar-valued or low-dimensional, it is usually feasible to store or transmit it as-is, uncom-pressed. However, once the size or dimensionality of the data exceeds a reasonable threshold, such as for image, au-dio, or video data, compression becomes necessary. The straightforward solution is to apply the mechanism 

M first and then losslessly compress the privatized output using, e.g., zip for general data or PNG for images. We con-sider lossless compression only, as using a lossy compressor could further compromise the utility of the privatized data in ways that are difficult to control. Denoting a lossless compressor as C, the compressed, privatized code of the data x is C(M (x)) . A benefit of this solution is that by the post processing property of DP mechanisms (Dwork & Roth, 2014), if M is ε-LDP, then so is C(M (x)) .However, in many practical situations, the privatize-then-compress solution quickly reaches a limit: since privacy mechanisms randomize the data, they usually introduce new, albeit random structure. As an extreme example, consider perfectly privatized image compression: we completely uni-formly randomize the pixels of the image. In this case, all in-formation is lost, but losslessly compressing each purely ran-dom pixel still requires the full bit depth of the image. This is sometimes referred to as the “communication-privacy-accuracy trilemma” (Chen et al., 2020). This counterintu-itive state of affairs is essentially conclusively resolved by the emerging field of stochastic codes (Flamich & G¨ und¨ uz, 2026), which we discuss next. Additionally, if the privacy mechanisms rely on continuous noise, such as Gaussian or Laplace noise, it is impossible to compress the privatized data losslessly; see (Shahmiri et al., 2024) for a resolution of this issue for Laplace noise. The inefficiency of the compress-then-privatize approach stems from its sequential structure: the compressor only ever handles the randomized data. In contrast, using stochastic codes allows us to jointly privatize and compress the data, leading to significant improvements in compression rate. Formally, given a conditional distribution PM |X , a stochas-tic code is a triplet (Z, enc , dec) made up of: • A random variable Z independent of the input X,shared between the sender/compressor and the re-ceiver/decompressor, and hence called the common randomness . In practice, Z can be realized by using a shared pseudo-random number generator (PRNG) and a common seed for the PRNG. • A function enc( x, z ) mapping an input x and a realiza-tion of Z to a finite-length bitstring, called the encoder .• A function dec( s, z ) mapping a bitstring s and a real-ization of Z to the support of M , called the decoder .Finally, to qualify as a stochastic code for PM |X , the triplet must satisfy the requirement that for all x we have 

dec(enc( x, Z ), Z ) ∼ PM |X=x (1) Note that in the above, the randomness arises purely from the synchronized random variable Z. Intuitively, one can think of s = enc( x, Z ) as encoding a single random sample from PM |X . Somewhat surprisingly, it can be shown that not only can we construct a stochastic code for virtually any conditional distribution PM |X , but we can do so such that the expected codelength E[|enc( X, Z )|] is close to the mutual information I[M ; X], which is optimal (Li & El Gamal, 2018; Li et al., 2024; Flamich, 2024). As we can construct a stochastic code to compress a sample from an arbitrary conditional distribution, and privacy mech-anisms may be conceived as conditional distributions, it is a natural next step to combine the two. However, things be-come more involved regarding the privacy certificate: with stochastic codes, we have the Markov chain 

X → enc( X, Z ) → dec(enc( X, Z ), Z )

| {z } 

> ∼M(X)

(2) as opposed to X → M (X) → C(M (X)) as we had with the privatize-then-compress solution. This observation shows that even if M (X) is ε-LDP, post processing does not apply and enc( X, Z ) is not immediately ε-LDP. Instead, we need to take special care in constructing (Z, enc , dec) 

so that it can retain (some of) the privacy guarantees of the mechanism M that it is compressing. The first works to construct such stochastic codes were Feld-man & Talwar (2021); Triastcyn et al. (2021) and Shah et al. (2022), however, they have suboptimal compression-privacy 3Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

performance. Instead, we base our work on the Poisson pri-vate representation (PPR; Liu et al., 2024), the only known stochastic code that exactly simulates arbitrary differential privacy mechanisms with optimal expected codelength. 

2.3. The Poisson Private Representation 

As we outlined above, to define the PPR code for an ϵ-LDP mechanism M implementing the conditional distribution 

PM |X , we need to specify the triplet (Z, enc , dec) that sat-isfies Equation (1). To begin, we assume that there is a marginal distribution QM over the support of M such that 

PM |X=x ≪ QM , i.e., the support of PM |X=x is contained in the support of QM for all x. Furthermore, we assume we can easily generate samples from QM and that we can compute the density ratio (technically, the Radon-Nikodym derivative) dP M |X 

> dQ M

(m | x). In our application to diffusion models, QM will be either a Gaussian or Laplace distribu-tion independent of X while PM |X will be a Gaussian or Laplace distribution that does depend on X, and the above conditions will be satisfied. Thus, we now define PPR. 

Definition 1 (Poisson Private Representation (Liu et al., 2024)) . Let PM |X be an ϵ-LDP mechanism and let QM

be a probability distribution such that its support contains the support of PM |X=x for all x. For convenience, let 

r(m | x) = dP M |X 

> dQ M

(m | x). Now, let n ∈ N and α > 1.Then, we define the PPR code as follows: • We set the common randomness as an infinite sequence of pairs of random variables Z ← { (Mk, T k)}∞

> k=1

,where Mk ∼ QM and (Tk − Tk−1) ∼ Exp(1) with the convention that T0 = 0 . Such an infinite sequence is called a Poisson process , the namesake of PPR. • We define the encoder in two steps. First, using random variables V = {Vk}∞ 

> k=1

where Vk ∼ Exp(1) are not known to the decoder, the encoder computes 

K(X, Z, V ) = arg min 

> k∈N

Vk · T αk · r(Mk | X)−α (3) Although the minimization is over an infinite set, it is nonetheless possible to find a minimizer in finite time. As K is an integer, we need to map it to a bitstring to get a valid stochastic code. To this end, we construct an arithmetic code ζ(k) from a zeta distribution, so that setting enc( X, Z, V ) = ζ(K(X, Z, V )) and ℓ =

DKL [PM |X ∥ QM ] + log(3 .56) / min {(α − 1) /2, 1},

E[|enc( X, Z )|] ≤ ℓ + log( ℓ + 1) + 2 (4) For the precise encoding details, see Liu et al. (2024). • Finally, the decoder simply recovers the selected index 

K from the ζ-code and then selects the sample MK

that the encoder computed from Z = {(Mi, T i)}∞

> i=1

:

dec( S, Z ) ← Mζ−1(S) (5) The rationale for the above definition of the PPR code is threefold: first, one can show that given our selection rule in Equation (3), we get MK(x,Z,V ) ∼ PM |X=x, and hence 

dec(enc( x, Z ), Z ) ∼ PM |X=x as required. Second, the expected codelength (Equation (4)) for any fixed α is close to optimal (Liu et al., 2024; Li et al., 2024). Third, Theorem 4.5 of Liu et al. (2024) shows that when the mechanism 

M (x) is ϵ-LDP, then enc( x, Z ) is 2αϵ -LDP, and hence, so is dec(enc( X, Z ), Z ) by the post processing property. Finally, we must deal with the runtime N (X, Z, V ) of PPR. It can be shown that PPR belongs to the class of 

selection samplers (Flamich & Wells, 2024), and thus, un-fortunately, its expected runtime upon input x is at least 

E[N (x, Z, V )] ≥ ∥ r(· | x)∥∞. While this quantity can generally be quite high, it is especially problematic for our intended application to diffusion models, as in this case, the expected runtime can be infinite because the density ratios involved can be unbounded! Thus, as our first contribution, in Section 3, we shall develop a variant of PPR that can com-press privacy mechanisms with unbounded density ratios at the cost of encoding them only approximately. However, as our Theorem 3.1 shows, the error of approximate PPR’s output is small in total variation, and the algorithm is guar-anteed to have finite runtime, a short codelength, and to retain its desirable privacy certificate. Indeed, our algorithm will have fixed runtime, which has the further benefit that we can parallelise the most expensive computations, which further improves the runtime. Unfortunately, for the approximation guarantee to hold, the runtime has to be O(exp( DKL [PM |X ∥ QM ])) , which is still prohibitively expensive for any practical privacy mech-anism. Thus, finally, we describe DiffC, a framework that enables scaling stochastic codes to high-resolution data by combining them with diffusion models. 

2.4. Data Compression with Diffusion Models 

A diffusion denoising probabilistic model (DDPM; Ho et al., 2020; Sohl-Dickstein et al., 2015) consists of a fixed forward noising process and a learned reverse denoising process. The forward process gradually corrupts a clean sample x0 ∼

q(x0) with Gaussian noise so that, at any time-step t ∈

[0 , T ], the marginal admits the closed form 

xt = γtx0 + σtηt, ηt ∼ N (0 , I) ,

where γt = p1 − σ2 

> t

and σ2 

> t

is a monotonically in-creasing noise schedule with σ0 ≈ 0 and σT ≈ 1.We write SNR( t) = γ2 

> t

/σ 2 

> t

for the signal-to-noise ra-tio at time-step t. The reverse process is parametrized as a Markov chain pθ (xs | xt) trained to minimize the per-step KL divergence DKL [q(xs | xt) ∥ pθ (xs | xt)] for 

s < t . Note that this objective is equivalent to minimizing 

DKL [q(xs | xt, x 0) ∥ pθ (xs | xt)] , where conditioning on 4Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

x0 yields a Gaussian posterior with closed form 

q(xs | xt, x 0) = N (μq (xt, x 0; s, t ), σ 2(s, t )I) (6) 

μq (xt, x 0; s, t ) = γsσ2

> t|s

σ2

> t

x0 + γt|sσ2

> s

σ2

> t

xt (7) where γt|s = γt 

> γs

, σ 2 

> t|s

= σ2 

> t

− γ2

> t|s

σ2 

> s

, and σ(s, t ) = σt|sσs 

> σt

.Theis et al. (2022) observed that the forward and reverse pro-cesses together admit a natural progressive lossy compres-sion interpretation: We can use a stochastic code to encode a sample xs from q(xs | xt, x 0) using the model proposal 

pθ (xs | xt), terminating at any intermediate timestep to trade off bitrate for distortion. Theis et al. (2022) called this framework DiffC and showed that it possesses surprisingly favorable rate-distortion performance, but did not imple-ment it. Indeed, it was first implemented by Vonderfecht & Liu (2025) using latent diffusion models, including Stable Diffusion 1.5/2.1/XL and FLUX.1-dev (Esser et al., 2024). Although the latter is a rectified flow model, stochastic sam-pling along its probability flow can be shown to be equiv-alent to DDPM sampling Lai et al. (2025), and hence we may use it for DiffC. Flamich & G ¨und ¨uz (2026) recently re-visited the path-based encoding in DiffC from the stochastic coding perspective. They observed that under the assump-tion of a perfect denoiser, the KL divergence between joint forward and reverse paths on any common discretization 

s = t0 < t 1 < · · · < t n = T equals the KL divergence between their endpoint marginals at s. This observation means that encoding the final denoising step and the entire denoising path has roughly the same compression cost us-ing stochastic codes. However, encoding the path is now practically feasible. Beyond porting DiffC to large pretrained models, Vonder-fecht & Liu (2025) introduced several engineering contribu-tions that bring encoding times down to below 10 seconds. They perform relative entropy coding in fixed-size chunks of approximately 16 bits of KL, splitting high-KL transitions across statistically independent subsets of dimensions, so that each chunk can be feasibly compressed with a stochastic code. Furthermore, they use a custom CUDA kernel for the stochastic code, which draws the candidate samples directly inside the kernel rather than materializing them in GPU memory. Finally, they cast the choice of denoising timestep schedule as a shortest path problem over the cost matrix of pairwise per-step KL divergences. As an ablation, they show that hard-coding the per-encoding-step KL budget across all images, rather than transmitting it as side information, has a negligible effect on the resulting rate-distortion curves. 

# 3. Approximate PPR 

As we mentioned in Section 2.2, PPR encodes an arbitrary LDP mechanism PM |X using a coding distribution QM with as short a codelength as possible. However, PPR’s exactness can be a drawback: its runtime scales as dP M |X 

> dQ M∞

, which can be infinite even when DKL [PM |X ∥ QM ] is finite. We address this issue by developing an approximate variant of PPR whose output is no longer an exact sample from PM |X

but is still suitably close, as measured by the total variation distance. Furthermore, this variant will retain its close-to-optimal expected codelength and its average runtime scales as O(exp( DKL [PM |X ∥ QM ])) .To achieve our goal, we adopt the deceptively simple strat-egy of Flamich & Wells (2024): we introduce a budget 

n ∈ N for the maximum number of samples PPR is al-lowed to examine. If the algorithm exceeds this budget, we terminate it and return the best index found so far. 

Definition 2 (Step-limited Poisson Private Representa-tion) . Let PM |X , Q M , r (m | x), Z = {(Mi, T i)}∞

> i=1

,

V = {Vi}∞ 

> i=1

and ζ the arithmetic code constructed from a zeta distribution as in Definition 1. In particular, recall that PM |X is an ϵ-LDP mechanism whose output we wish to encode. Now, let n ∈ N and α > 1. We define the step-limited PPR algorithm as follows: 1. The common randomness is Z = {(Mi, T i)}∞

> i=1

.2. For the encoder, we define the step-limited index 

Kαn (x, Z, V ) = arg min 

> k∈[1: n]



Vk

 Tk

r(Mk | x)

α

(8) Then, we set the encoder as 

enc αn (x, Z, V ) = ζ(Kαn (x, Z, V )) (9) 3. The decoder is the same as in Definition 1: 

dec( s, Z ) = Mζ−1(s) (10) We see that by definition, the runtime of step-limited PPR is finite. But how should we choose the budget n? Besides proving that our algorithm retains PPR’s LDP certificate and close-to-optimal codelength, the following result shows that choosing n = O(exp( DKL [PM |X ∥ QM ])) ensures that the algorithm’s output distribution will be close to that of the original mechanism. 

Theorem 3.1. Let PM |X , Q M , K αn , V and let (Z, enc , dec) 

be the step-limited PPR algorithm as in Definition 2. Let 

n ∈ N be the step-limit and α > 1 be the PPR parameter. Take ℓα(X) = DKL [PM |X ∥ QM ]+ log 3 .56 min {(α−1) /2,1} . Then, 1. LDP guarantee: enc αn (x, Z, V ) is 2αϵ -LDP. 2. Codelength: Akin to exact PPR, we have for all x:

E[|enc αn (x, Z, V )|] ≤ ℓα(x) + log( ℓα(x) + 1) + 2 

5Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

3. Approximation quality: Let β > 0 and let Q(n)  

> M|X

de-note the distribution of dec(enc αn (x, Z, V ), Z ). Then, choosing n ≥ 2ℓα(X)/β guarantees that 

DTV [Q(n)  

> M|X=x

∥ PM |X=x] ≤ β (11) 

Proof. LDP guarantee. Let x, x ′ be two inputs. Since the mechanism M is ϵ-LDP by assumption, we have 

e−ϵr(m | x′) ≤ r(m | x) ≤ eϵr(m | x′) (12) Next, by a standard fact about the argmin of scaled expo-nential random variables (see Section 3.3 of Li et al. (2024), for example), we have 

P[Kαn (x, Z, V ) = k | Z] = T αk · r(Mk | x)−α

Pnj=1 T αj · r(Mj | x)−α

≤ T αk · (e−ϵr(Mk | x′)) −α

Pnj=1 T αj ·(eϵr(Mj | x′)) −α

= e2αϵ T αk · r(Mk | x′)−α

Pnj=1 T αj · r(Mj | x′)−α

= e2αϵ P[Kαn (x′, Z, V ) = k | Z]

as desired. 

Codelength. By its definition Kαn (x, z, v ) ≤ K(x, z, v ),and hence we have that 

E[log Kαn (x, Z, V )] ≤ ℓα(x) (13) Thus, using our arithmetic code ζ to encode Kαn (x, Z, V ),we get the desired result. 

Approximation quality. We use a technique similar to that of Flamich & Wells (2024): Let K(X, Z, V ) be the index of exact PPR defined in Equation (3). First, note that (e.g., using the technique from Section 4.3.2 of Flamich (2024)): 

DTV [Q(n)  

> M|X=x

∥ PM |X=x] ≤ P[K(X, Z, V ) > k ]

By Theorem 4.3 of Liu et al. (2024) we have for ℓ(X) = 

DKL [PM |X ∥ QM ] that 

E[log Kαn (X, Z, V )] ≤ ℓα(X) (14) Since log K(X, Z, V ) ≥ 0, we may apply Markov’s in-equality to obtain 

P[K(X, Z, V ) > n ] ≤ ℓα(X)log( n + 1) (15) Therefore, we see that 

DTV [Q(n)  

> M|X=x

∥ PM |X=x] ≤ ℓα(X)log( n + 1) (16) Thus, setting n ≥ 2 ℓα (X) 

> β

yields the desired approximation guarantee. 

# 4. Privatized Data Compression with Diffusion 

4.1. The Denoising Process as a Privacy Mechanism 

Our most important high-level observation is that, the DDPM denoising process conditioned on the original image 

x0 is a valid privacy mechanism . Concretely, at each denois-ing timestep, we may view a sample from the conditional posterior given in Equation (6) as an additive Gaussian pri-vacy mechanism for x0:

M (x0) = μq (xt, x 0; s, t ) + σ(s, t ) η, η ∼ N (0 , I) (17) We defer the discussion on the privacy guarantee of this mechanism to Appendix A. Unfortunately, the Gaussian mechanism does not yield a 

pure ϵ-LDP certificate, and hence our Theorem 3.1 does not apply if we apply step-limited PPR to it. To circumvent this issue, we moment-match Laplace distributions to the Gaussian targets and proposals at each time step and use those for denoising and compression instead. Thus, the privacy mechanism becomes 

M (x0) = μq (xt, x 0; s, t ) + b(s, t ) η, η ∼ L (0 , I) (18) where b(s, t ) = σ(s, t )/√2. For a discussion of high-dimensional Laplace distribution on differential privacy, see (Andr´ es et al., 2013). In what follows, we aim to provide per-pixel guarantees for image data. Thus, x(i)0 will denote a pixel on an image, consisting of C channels (e.g., C = 3 for RGB), with x(i,c )0

denoting channel c. We start with per-subpixel guarantees, which we also refer to as “per-channel,” from which we recover the per-pixel guarantees. Following the steps in Dwork & Roth (2014, Theorem 3.6) applied to a single channel, the per-channel mechanism satisfies ϵch -DP where 

ϵch = γsσ2

> t|s

σ2 

> t

b(s, t ) · sup |x(i,c )0 − x′ (i,c )0 | (19) and the supremum is taken over all possible images. This admits the nicer form (see Appendix A for details): 

ϵch = p2( SNR (s) − SNR (t)) sup |x(i,c )0 − x′ (i,c )0 |

By basic composition over the C channels of a pixel, the per-pixel ϵt→s-DP guarantee per step is 

ϵt→s = Cp2( SNR (s) − SNR (t)) sup |x(i,c )0 − x′ (i,c )0 |

4.2. Compressing the Privacy Mechanism 

In DiffC (Theis et al., 2022), the output of each denoising step is not released directly, but compressed via a stochastic code; in our case, this is where we will apply the PPR 6Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

code. Under PPR, at each step we compress the privacy mechanism Mt→s(x0) and the compressed output inherits a privacy guarantee that depends on PPR’s parameter α and the privacy parameters of the mechanism being compressed. Note that in the works of Theis et al. (2022) and Vonderfecht & Liu (2025), the code they used is equivalent to our PPR code with the parameter α = ∞, which achieves maximal compression efficiency but provides no privacy guarantee. However, as we explained in Section 2.3, exact PPR (that is, the budget is n = ∞) requires an upper bound on the density ratio between the target and the proposal distributions. In the Gaussian case of Equation (17), both distributions are Gaussians with the same variance 

q = N (μq , σ 2I) , p = N (μp, σ 2I) , (20) whose density ratio is easily seen to be unbounded. For-tunately, with our moment-matched Laplace distributions (Equation (18)) this is no longer an issue: the density ratio of two Laplace distributions with the same scale is always bounded. However, despite the density ratio no longer being an issue, step-limited PPR still provides a significant compu-tational advantage compared to its exact variant. Since we choose the step-budget n ahead of time, we can draw the n

proposal samples and evaluate their density ratios in parallel on a GPU using a custom CUDA kernel (see Appendix B). Thus, we use step-limited PPR (Definition 2). Combining it with the per-step, per-pixel guarantee derived in Section 4.1, the PPR code satisfies a 2αϵ t→s-LDP per-pixel guarantee at each step. By sequential composition, the total per-pixel privacy certificate across n encoding steps is: 

ϵtotal = 2 α

> n

X

> i=1

ϵti→ti−1

We show experimentally that, for compression to the same final timestep, replacing the Gaussian channel with the moment-matched Laplace channel and α = ∞ with α = 2 

in step-limited PPR has a negligible effect on the down-stream task, while increasing the bitrate by a factor of ≈ 2;see the bottom left panel of Figure 1. The factor 2 increase can be in large part explained by comparing the relative entropies between the original Gaussians involved and their moment-matched Laplace counterparts, see Appendix C. 

# 5. Experiments 

We compare the privacy-utility-compression tradeoff of DP-DiPP against the privatize-then-compress baseline and DiffC (Theis et al., 2022) for training an image classifier on privatized and compressed CIFAR-10 images, taking final classification accuracy as our utility function. Concretely, we add calibrated Laplace noise to the image and compress the result with the PNG lossless image com-pression codec. Both methods satisfy per-pixel pure ϵ-DP, so their utility and compression rate can be compared at the same privacy level. We also compare against the original DiffC to understand how replacing the Gaussian channel with the Laplace channel as well as using α = ∞ versus 

α = 2 for PPR affects utility and coding cost. 

5.1. Privatization 

For privatization, our encoding pipeline modifies DiffC in two ways: we sample from the moment-matched Laplace channel ( b = σ/ √2), and we compress this channel with PPR using α = 2 . At each encoding step, our CUDA kernel samples from zi

> iid

∼ L (0 , 1) and computes the logarithm of density ratios evaluated at those samples as P

> i

(|zi| − |zi − μi|) (except the constant term), differently from the implementation in Vonderfecht & Liu (2025) for PFR, which is P 

> i

ziμi (here μi = ( μq − μp)/b ). The encoder then uses these precomputed density ratios to run step-limited PPR (Definition 2). To decode, the receiver, sharing the same random seed, retrieves the selected sample and reconstructs 

xs = b · z + μp using the diffusion model’s noise prediction 

μp(xt, η θ ; s, t ) = 1

γt|s

xt − σ2

> t|s

σt

ηθ (xt, t )

!

Each encoded image produces two outputs. The noisy re-construction is the raw output from the final encoding step; it lies at an intermediate diffusion timestep and contains visible noise. The denoised reconstruction is obtained by running DDPM from this final timestep to t = 0 .The encoding schedule is chosen by solving a resource-constrained shortest-path problem over the timesteps, ex-tending the method used in Vonderfecht & Liu (2025), which does not have a privacy constraint. The aim is to find the schedule that minimizes the total coding cost sub-ject to the privacy budget ϵtotal for each final timestep Tfinal .Then, setting ∆ch = sup |x(i,c )0 − x′ (i,c )0 | = 2 , encoding each denoising step consumes 

ϵPPR (t → s) = 6 αp2( SNR (s) − SNR (t))∆ ch 

units of per-pixel privacy budget. For the Laplace noising baseline, we add independent 

L(0 , ∆′

> ch

/(ϵ/ 3)) noise to each channel where ∆′ 

> ch

= 255 ,clamp to [0 , 255] , and compress losslessly with PNG .

5.2. Classification 

The pretrained DDPM model (Ho et al., 2020) used as the backbone of our compression schemes has been trained on the CIFAR-10 training set. Hence, to prevent data leakage, we use the CIFAR-10 test set, split evenly into 5k training and 5k evaluation images to train and evaluate our classifier. 7Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

0 5 10 15 20 

20 

40 

60 

80 

Rate (bpp) 

> Accuracy (%)

0 100 200 300 

20 

40 

60 

80 

ε-LDP certificate 

> Accuracy (%)

0 500 1,000 

0

5

10 

15 

ε-LDP certificate 

> Rate (bpp)

0 0.5 1 1.5 2

20 

40 

60 

Rate (bpp) 

> Accuracy (%)

0 100 200 300 

20 

40 

60 

ε-LDP certificate 

> Accuracy (%)

0 100 200 300 

0

0.5

1

ε-LDP certificate    

> Rate (bpp)
> Uncompressed, unprivatized Gaussian DiffC, unprivatized PNG-compressed Laplace mechanism DP-DiPP (ours)

Figure 1. Experimental results on training an image classifier from a dataset of privatized and compressed CIFAR-10 images. Note that Gaussian DiffC does not have an LDP certificate and is included only for illustration. The left and middle columns also include the classification accuracy from the original, unprivatized data for comparison. In each case, the markers are connected by lines in order of increasing ϵ, where ϵ is the method’s LDP guarantee. Left column: The rate-utility tradeoff, as measured by bits per pixel (bpp) and test classification accuracy, respectively. Middle column: Privacy-utility tradeoff, where the method’s LDP certificate measures privacy. 

Right column: Rate-privacy tradeoff. Top row: comparisons among 1) the privatize-then-compress method using 2) Gaussian DiffC and 3) DP-DiPP. Bottom row: Same comparisons as top row with the privatize-then-compress method omitted. 

To measure how privacy affects downstream utility, we fol-low the protocol of Cao et al. (2026) and train a ResNet-56 classifier directly on the privatized images. The classifier never sees clean data; it is trained in the noisy domain using the provided image labels, using the same hyperparameters as Cao et al. (2026) to enable direct comparison. 

5.3. The DiffC Baseline 

We compare DP-DiPP to the original Gaussian DiffC (Von-derfecht & Liu, 2025), which is equivalent to our step-limited PPR algorithm with α = ∞. Note that Gaussian noise lacks a pure LDP guarantee, and so does using PPR with α = ∞; we include it as an ablation to show that DP-DiPP’s performance does not degrade significantly when using moment-matched Laplace distributions instead of the original Gaussians. To ensure a fair comparison between DiffC and DP-DiPP, we first solve the resource-constrained shortest path problem for the DP-DiPP mechanism at each privacy level ϵ. After we decide on a final timestep Tfinal ,we find the shortest path for DiffC that leads to Tfinal .

5.4. Empirical Findings 

Our experimental results are shown in Figure 1. Each point on the curve represents a final compression timestep, after which each method uses the DDPM denoiser to complete denoising the images. For our experiments, we tuned each method to attain a per-pixel ϵ-LDP certificate (or equivalent in the case of DiffC) for ϵ ∈ { 1, 4, 16 , 64 , 256 }. For the step-limited PPR code in DP-DiPP, we set α = 2 .First and foremost, our findings validate our method: DP-DiPP’s joint privatization and compression significantly out-perform the privatize-then-compress baseline, as shown in the top row of Figure 1. In particular, DP-DiPP achieves the same classification accuracy (top left panel) and the same LDP guarantee (top right panel) using less than 3-11% of the bits that the baseline method needs, yielding a 10-30 ×

improvement in the rate! Second, in the bottom row of Figure 1, comparison of DiffC and DP-DiPP shows that using moment-matched Laplace distributions instead of Gaussians and a strict privacy param-eter of α = 2 introduces a roughly 2-times penalty in the rate (bottom right), while the utility is largely unaffected. 

# 6. Discussion and Future Directions 

We introduced an approximate version of PPR and showed that it retains an LDP guarantee and has good approximation properties. Then, we applied it to diffusion-based compres-sion, and showed that using Laplace instead of Gaussian denoising distributions can significantly improve the formal privacy guarantee while retaining empirical performance. We applied our method to image compression on CIFAR-10 and showed that our joint compression method significantly outperforms the privatize-then-compress baseline. We have only dealt with pure ϵ-LDP mechanisms. Thus, a future theoretical direction could be to consider whether it is possible to construct approximate versions of PPR from mechanisms that are (ϵ, δ )-LDP instead. 8Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

# Impact Statement 

This paper presents work aimed at advancing the field of Machine Learning. There are many potential societal con-sequences of our work, none of which we feel must be specifically highlighted here. 

# Acknowledgements 

The authors acknowledge financial support from Imperial College London through an Imperial College Research Fellowship grant awarded to GF, and from the UKRI for projects AI-R (ERC Consolidator Grant, EP/X030806/1) and INFORMED-AI (EP/Y028732/1). 

# References 

Andr ´es, M. E., Bordenabe, N. E., Chatzikokolakis, K., and Palamidessi, C. Geo-indistinguishability: Differential privacy for location-based systems. In Proceedings of the 2013 ACM SIGSAC conference on Computer & commu-nications security , pp. 901–914, 2013. Balle, B. and Wang, Y.-X. Improving the Gaussian mecha-nism for differential privacy: Analytical calibration and optimal denoising. In International conference on ma-chine learning , pp. 394–403. PMLR, 2018. Cao, Y., Li, C., and He, W. LDP-Slicing: Local differential privacy for images via randomized bit-plane slicing. In 

Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2026. Chen, W.-N., Kairouz, P., and Ozgur, A. Breaking the communication-privacy-accuracy trilemma. Advances in Neural Information Processing Systems , 33:3312–3324, 2020. Dwork, C. and Roth, A. The algorithmic foundations of dif-ferential privacy. Foundations and trends® in theoretical computer science , 9(3-4):211–487, 2014. Dwork, C., McSherry, F., Nissim, K., and Smith, A. Calibrat-ing noise to sensitivity in private data analysis. In Theory of cryptography conference , pp. 265–284. Springer, 2006. Erlingsson, ´U., Pihur, V., and Korolova, A. Rappor: Randomized aggregatable privacy-preserving ordinal re-sponse. In Proceedings of the 2014 ACM SIGSAC con-ference on computer and communications security , pp. 1054–1067, 2014. Esser, P., Kulal, S., Blattmann, A., Entezari, R., M ¨uller, J., Saini, H., Levi, Y., Lorenz, D., Sauer, A., Boesel, F., et al. Scaling rectified flow transformers for high-resolution image synthesis. In Forty-first international conference on machine learning , 2024. Feldman, V. and Talwar, K. Lossless compression of ef-ficient private local randomizers. In International Con-ference on Machine Learning , pp. 3208–3219. PMLR, 2021. Flamich, G. Data Compression with Relative Entropy Cod-ing . PhD thesis, Apollo - University of Cambridge Repos-itory, 2024. URL https://www.repository. cam.ac.uk/handle/1810/385303 . PhD Thesis. Flamich, G. and G ¨und ¨uz, D. Data compression with stochas-tic codes. IEEE BITS the Information Theory Magazine ,pp. 1–14, 2026. doi: 10.1109/MBITS.2026.3702602. Flamich, G. and Wells, L. Some notes on the sample com-plexity of approximate channel simulation. In 2024 IEEE International Symposium on Information Theory Work-shops (ISIT-W) , pp. 1–6. IEEE, 2024. Ho, J., Jain, A., and Abbeel, P. Denoising diffusion proba-bilistic models. Advances in neural information process-ing systems , 33:6840–6851, 2020. Kairouz, P., Oh, S., and Viswanath, P. The composition theo-rem for differential privacy. In Bach, F. and Blei, D. (eds.), 

Proceedings of the 32nd International Conference on Ma-chine Learning , volume 37 of Proceedings of Machine Learning Research , pp. 1376–1385, Lille, France, 07– 09 Jul 2015. PMLR. URL https://proceedings. mlr.press/v37/kairouz15.html .Lai, C.-H., Song, Y., Kim, D., Mitsufuji, Y., and Ermon, S. The principles of diffusion models. arXiv preprint arXiv:2510.21890 , 2025. Li, C. T. and El Gamal, A. Strong functional representa-tion lemma and applications to coding theorems. IEEE Transactions on Information Theory , 64(11):6967–6978, 2018. Li, C. T. et al. Channel simulation: Theory and applications to lossy compression and differential privacy. Found. and Trends in Comm. and Info. Theory , 21(6):847–1106, 2024. Liu, M. F., Lyu, S., Vinaroz, M., and Park, M. Differ-entially private latent diffusion models. arXiv preprint arXiv:2305.15759 , 2023. Liu, Y., Chen, W.-N., Ozgur, A., and Li, C. T. Universal exact compression of differentially private mechanisms. 

Advances in Neural Information Processing Systems , 37: 91492–91531, 2024. Shah, A., Chen, W.-N., Balle, J., Kairouz, P., and Theis, L. Optimal compression of locally differentially private mechanisms. In International Conference on Artificial Intelligence and Statistics , pp. 7680–7723. PMLR, 2022. 9Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

Shahmiri, A. M., Ling, C. W., and Li, C. T. Communication-efficient laplace mechanism for differential privacy via random quantization. In ICASSP 2024-2024 IEEE In-ternational Conference on Acoustics, Speech and Signal Processing (ICASSP) , pp. 4550–4554. IEEE, 2024. Sohl-Dickstein, J., Weiss, E., Maheswaranathan, N., and Ganguli, S. Deep unsupervised learning using nonequi-librium thermodynamics. In International conference on machine learning , pp. 2256–2265. pmlr, 2015. Theis, L., Salimans, T., Hoffman, M. D., and Mentzer, F. Lossy compression with Gaussian diffusion. arXiv preprint arXiv:2206.08889 , 2022. Triastcyn, A., Reisser, M., and Louizos, C. DP-REC: Pri-vate & communication-efficient federated learning. arXiv preprint arXiv:2111.05454 , 2021. United States Census Bureau. Disclosure avoidance for the 2020 census: An introduction. Handbook, US Govern-ment Publishing Office, Washington, DC , 2021. Vonderfecht, J. and Liu, F. Lossy compression with pretrained diffusion models. arXiv preprint arXiv:2501.09815 , 2025. 10 Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

# A. Notes on Privacy Guarantee of Gaussian Diffusion 

We can investigate the privacy guarantee for the Gaussian diffusion process, continuing from Equation (17). The ℓ2

sensitivity of this mechanism is 

∆t→s = γsσ2

> t|s

σ2

> t

sup 

> x0,x ′
> 0∈X

∥x0 − x′

> 0

∥2

= γsσ2

> t|s

σ2

> t

DX .

and the ∆/σ ratio simplifies to 

∆t→s

σ(s, t ) = γsσ2

> t|s

/σ 2

> t

σt|sσs/σ t

DX

= γsσt|s

σtσs

DX

=

s γs

σs

2

−

 γt

σt

2

DX

= pSNR (s) − SNR (t)DX .

The (ϵ, δ )-differential privacy of this mechanism at each time step can be computed tightly using the result of Balle & Wang (2018, Theorem 8). The privacy guarantee of the full multi-step mechanism, which is the composition of per-step mechanisms, can then be obtained via an adaptive composition theorem such as Kairouz et al. (2015, Theorem 3.5). 

# B. Custom CUDA Kernel for PPR 

Different from the implementation released by the authors of Vonderfecht & Liu (2025), to sample from a moment matched laplace distribution at each timestep, we first generate laplace samples with mean 0 and scale 1. We draw these samples using inverse transform sampling:  

> 1

__device__ float curand_laplace_standard(curandState* state) { 

> 2

float u = curand_uniform(state);  

> 3

float half_minus = u - 0.5f;  

> 4

float sign_val = (half_minus >= 0.0f) ? 1.0f : -1.0f;  

> 5

float abs_val = fabsf(half_minus);  

> 6

float inner = fmaxf(1.0f - 2.0f * abs_val, 1e-10f);  

> 7

return -sign_val * logf(inner); // Laplace(0, 1)  

> 8

}

Also differently from Vonderfecht & Liu (2025), the log density ratio ( log w(z)) between target L(μ, 1) (where μ = μq −μp 

> b

)and proposal L(0 , 1) analytically becomes 

log w(z) = |z| − | z − μ|

thus we generate these ratios as:  

> 1

float log_w_value = 0.0f;  

> 2

for (int i = 0; i < dim; i++) { 

> 3

float z = curand_laplace_standard(&state);  

> 4

log_w_value += fabsf(z) - fabsf(z - mu_q[i]);  

> 5

}

We decide on the sample returned by step limited PPR using: 11 Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes  

> 1

t = cp.random.exponential(scale=1.0, size=K)  

> 2

v = cp.random.exponential(scale=1.0, size=K)  

> 34

s = alpha * (log_cumsum_t - log_w) + log_v  

> 56

winning_seed = cp.argmin(s).item()  

> 7

sample = generate_sample(dim, shared_seed, winning_seed) 

We note that we can convert samples from Zi

> iid

∼ L ( μq −μp 

> b

, 1) to Xi

> iid

∼ L (μq , b ) by 

Xi = Zi · b + μp,

where both μp and b are available to both the encoder and decoder. This is implemented in the code as  

> 1

mu = ((qmu[i] - pmu[i]) / b).flatten().detach().cpu().numpy()  

> 2

sample, cs, dkl = ch_sim.encode(mu, manual_dkl=manual_dkl[si], seed=si)  

> 3

seeds_all[i].append(cs); dkl_all[i].append(dkl)  

> 4

xl[i] = torch.tensor(sample).reshape(xl[i].shape).to(dev).to(dt) * b + pmu[i] 

# C. Explanation of the Factor Two Increase in Bitrate after Moment Matching 

We illustrate the phenomenon for one-dimensional distributions. As the distributions involved in practice are fully factorised dimensionwise, the conclusion carries over by the chain rule of relative entropies. Thus, consider now 

q = N (μq , σ 2), p = N (μp, σ 2),q′ = L(μq , σ/ √2) , p′ = L(μp, σ/ √2) 

where q, p represent one dimension of the conditional and unconditional denoising distributions from Equation (20), and 

q′, p ′ are their moment-matched counterparts. For convenience, let δ = |μp−μq | 

> σ

be the standardised absolute mean deviation. Then, using the well-known formulae for the relative entropy of two Gaussian and two Laplace distributions, we get 

DKL [q ∥ p] · ln 2 = δ2

2 (21) 

DKL [q′ ∥ p′] · ln 2 = √2δ − 1 + e−√2δ (22) 

= √2δ − 1 + 1 − √2δ + δ2 + O(δ3) (Taylor expansion around 0)

= δ2 + O(δ3) (23) Comparing Equations (21) and (23), we see that for small δ, we indeed have DKL [q′ ∥ p′] ≈ 2 · DKL [q ∥ p], as we observe in practice. We corroborate this empirically, by studying the distribution of δ across the dimensions of our diffusion models for different ϵ-LDP settings in Figure 2. We see that for all settings, the distribution of δ is heavily skewed towards 0, with barely any values being close even to 1; thus our Taylor approximation is roughly valid. 12 Scalable Differentially Private Data Compression via Diffusion and Stochastic Codes 

Figure 2. Per ϵ distribution of the standardised absolute mean deviation δ. Each box pools δ over all time steps of the PPR schedule, all 128 test images, and all 32 × 32 × 3 latent dimensions. Box = interquartile range, line = median, whiskers = 5th/95th percentiles. 

13
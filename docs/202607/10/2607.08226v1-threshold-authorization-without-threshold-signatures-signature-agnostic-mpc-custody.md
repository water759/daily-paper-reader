---
title: "Threshold Authorization Without Threshold Signatures: Signature-Agnostic MPC Custody"
title_zh: 无阈值签名的阈值授权：签名无关的MPC托管
authors: Dariia Porechna
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08226v1"
tags: ["query:sec-cloud-ai"]
score: 9.0
evidence: 基于MPC和Shamir秘密共享的阈值授权方案，无需阈值签名，适用于密钥托管。
tldr: 传统数字资产托管依赖门限签名方案(TSS)实现多签授权，但后量子时代标准化哈希签名难以高效门限签名，格基方案仍在研究。本文提出双门架构，成员用普通EUF-CMA签名批准，门限通过Shamir秘密共享产生密封，签名方案可替换（如ECDSA换SLH-DSA），仅需密钥轮换。架构支持编程授权策略，安全性信息论，低于门限的敌手即使持有足够签名密钥也无法伪造密封。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08226v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1128, \"height\": 1062, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08226v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1200, \"height\": 816, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08226v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1064, \"height\": 1077, \"label\": \"Table\"}]"
motivation: 后量子迁移使传统门限签名方案难以适用，需一种签名无关的门限授权方案。
method: 提出双门架构，分离成员认证（普通签名）与门限授权（Shamir共享密钥产生密封），签名方案作为可替换参数。
result: 实现了签名无关的门限授权，支持后量子签名替换，无需协议重设计，安全性信息论。
conclusion: 为数字资产托管提供灵活、抗量子、签名无关的门限授权架构，可部署于智能合约或HSM。
---

## 摘要
数字资产托管建立在阈值多方批准之上：只有n方中的t方批准才能进行操作，且少于t个被攻破的方既不能授权也不能学习授权秘密。阈值签名方案（TSS）一直是标准机制，但后量子过渡打破了这一模式：标准化的基于哈希的签名难以实现高效的阈值签名，而基于格上的阈值协议仍是新兴研究方向。我们提出了一种双门架构，将成员认证与阈值授权分开。每个成员使用任何EUF-CMA方案下的普通签名签署其批准；法定人数共同根据与操作绑定的Shamir共享秘密生成阈值印记。该印记是可编程授权计算的基础实例：简单法定人数是最小策略，而更丰富的策略可以评估秘密共享状态，而无需将成员签名方案作为该计算的一部分。签名方案是部署参数：从ECDSA迁移到SLH-DSA或ML-DSA是一次密钥轮换，而非协议重新设计，且在商用HSM中持有密钥的成员通过标准签名API参与。该架构可部署在资产控制路径支持可编程验证的任何地方，例如智能合约、保险库模块或保护主密钥的HSM，并生成执行层授权而非原生链签名。低于阈值的秘密是信息论安全的；持有≥t个签名密钥但没有系数份额的对手仍然无法生成印记。

## Abstract
Digital-asset custody has been built on threshold multi-party approval: no operation proceeds unless $t$ of $n$ parties approve, and fewer than t compromised parties can neither authorize nor learn the authorization secret. Threshold signature schemes (TSS) have been the standard mechanism, but the post-quantum transition disrupts this model: standardized hash-based signatures resist efficient threshold signing, and lattice-based threshold protocols remain an emerging research track.   We present a dual-gate architecture that separates member authentication from threshold authorization. Each member signs its approval with an ordinary signature under any EUF-CMA scheme; the quorum jointly produces a threshold seal from Shamir-shared secrets bound to the operation. The seal is the base instance of a programmable authorization computation: simple quorum is the minimal policy, while richer policies can evaluate secret-shared state without making the member-signature scheme part of that computation. The signature scheme is a deployment parameter: migrating from ECDSA to SLH-DSA or ML-DSA is a key rotation, not a protocol redesign, and members holding keys in commodity HSMs participate through the standard sign API. The architecture can be deployed wherever the asset-control path supports programmable verification, such as smart contracts, vault modules, or HSMs guarding a master key, and produces an enforcement-layer authorization rather than a native chain signature. Below-threshold secrecy is information-theoretic; an adversary holding $\geq t$ signing keys but no coefficient shares still cannot produce the seal.

---

## 论文详细总结（自动生成）

以下是对论文《Threshold Authorization Without Threshold Signatures: Signature-Agnostic MPC Custody》的详细中文总结。

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：数字资产托管需要阈值多方批准（t-of-n），传统上依靠阈值签名方案（TSS）同时实现成员认证和阈值授权。然而，后量子迁移打破了这一模式：  
  - 标准化哈希签名（如SLH-DSA）无法高效实现阈值签名（需在MPC内评估哈希函数，成本过高）。  
  - 格基阈值签名（如ML-DSA）仍在研究阶段，未达到生产基线。  
  - 因此，需要一种不依赖阈值签名本身即可提供阈值授权能力的新架构，使签名方案成为可替换的部署参数，从而平滑后量子过渡。
- **整体含义**：提出一种“双门架构”（dual-gate architecture），将成员认证（普通EUF-CMA签名）与阈值授权（Shamir共享秘密产生的印记）解耦，实现签名无关的MPC托管。该架构可使从ECDSA迁移到SLH-DSA或ML-DSA仅需密钥轮换，无需协议重设计，且支持HSM等现有基础设施。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：授权由两个独立门组成：  
  - **签名门（Signature Check）**：每个成员使用普通签名签署其批准，验证者检查签名是否有效。  
  - **印记门（Seal Check）**：通过Shamir秘密共享在设置阶段分发系数份额，操作时各成员对共同标量x计算局部评估值 \([σ_ν]_i = [k_1]_i·x + [k_2]_i\)，收集t个有效份额后可Lagrange插值重建最终印记σ_ν。  
  - 只有两个门都通过才授权。窃取签名密钥但无系数份额不能生成印记；窃取系数份额但无有效签名不能通过签名门。
- **关键技术细节**：  
  - **秘密共享**：使用Shamir (t, n) 门限，每个操作对应一个单次使用的系数槽（coefficient slot），槽内两个系数(k₁, k₂)通过JRSS（Joint Random Sum Sharing）无经销商地生成。  
  - **印记构造**：采用Wegman-Carter一次认证器，σ_ν = k₁·x + k₂，其中x由操作消息和槽id派生。  
  - **消息绑定**：通过哈希计算规范消息μ = H(“custody-op” ∥ 地址 ∥ 策略id ∥ 操作类型 ∥ ν ∥ H(M))，确保印记与具体操作绑定。  
  - **份额正确性**：设置阶段发布份额的哈希承诺（带随机盐），操作时成员需打开承诺，验证者检查打开是否正确。  
  - **可编程授权**：简单法定人数是基例，可扩展为加权、分层、基于风险等更丰富的策略，通过秘密共享状态评估，无需暴露单个成员秘密。
- **流程描述**（文字说明）：
  1. **设置**：每个槽ν，成员执行JRSS，将随机a_j, b_j公开，求和得到(k₁_i, k₂_i)份额；记录份额承诺到设置根，注册成员公钥。
  2. **批准**（Approve）：成员检查本地策略，保留槽ν，计算局部印记[σ_ν]_i，形成信封（包括μ、槽id、成员id、评估值、份额打开），用其私钥签名。
  3. **授权**（Authorize）：中继收集≥t个签名信封，提交给执行层。执行层验证成员签名（C1），验证份额打开正确且评估一致（C2），插值重建印记σ_ν（C3），检查槽未消耗（C4）。成功后消耗该槽。
  4. **维护**（Maintain）：份额刷新、成员密钥轮换等，本身也是授权操作。

## 3. 实验设计

- **验证方式**：论文主要依赖形式化安全证明及安全实验，而非传统机器学习或基准测试。  
  - **形式化证明**（附录A、B）：定义阈值不可伪造性（TA-UF）、未使用槽保密性（TA-Priv）、属性归因等安全实验，并通过归约到签名EUF-CMA安全性、承诺打开不可伪造性和哈希碰撞抗性给出具体界限。  
  - **攻击矩阵**（表1）：列举代表性对手（如仅窃取一个签名密钥、窃取≥t个签名密钥但无份额、完全攻破<t个成员等），分析其能否授权。  
  - **对比分析**（表2）：将双门架构与阈值ECDSA在工作流视角（Setup、Approve、Authorize、Verify、Maintain、Output）进行对比。  
  - **参考实现**（Section 8）：使用Rust实现完整协议，包含secp256k1标量域、Ed25519签名、三种份额正确性轮廓（哈希承诺、Pedersen承诺、外部AVSS适配器）。测试覆盖：Shamir轮转、快乐路径（2-of-3, 3-of-5）、法定人数一致性、消息绑定、抵抗“密钥无份额”攻击、抵抗篡改评估、标准拒绝情况。  
- **无传统数据集/benchmark**：论文未使用标准ML数据集，也未与现有阈值签名方案（如GG18、FROST）进行交叉实验性能对比。对比是概念性的。
- **对比方法**：与阈值签名（TSS）、链上多签（multisig）、链下双重控制（off-chain dual control）分别比较了优势与不足。

## 4. 资源与算力

- **论文未明确说明使用的算力资源**（如GPU型号、数量、训练时长）。参考实现是以不依赖特定加速硬件的Rust代码给出，测试在常规处理器上即可运行，未报告具体硬件规格或运行时间。  
- 可推断：由于协议仅涉及标量域运算和哈希，计算开销很低，无需大规模算力。

## 5. 实验数量与充分性

- **实验数量**：  
  - 安全证明：三个主要定理（Theorem 1、2）和两个命题（Proposition 1、2），包含完整的归约。  
  - 参考实现：测试套件覆盖多种场景（快乐路径、安全拒绝、篡改检测等），但未报告精确测试用例数。  
  - 攻击矩阵：覆盖6种代表性对手情况。
- **充分性评估**：  
  - **正面**：形式化模型严谨，归约到标准假设（EUF-CMA、承诺打开不可伪造、碰撞抗性），攻击矩阵覆盖了关键威胁。参考实现验证了功能正确性。  
  - **不足**：缺少与生产级阈值签名方案的定量性能对比（如延迟、计算开销、带宽）；未在真实多成员、异步网络环境中进行实验；借鉴VSS/AVSS的细节未实验验证（开放问题部分提到）。总体而言，论文在安全分析上充分，但实验验证偏向概念验证，工程充分性有待进一步评估。

## 6. 论文的主要结论与发现

- **双门架构可行**：可以在不阈值化签名本身的情况下，实现与阈值签名等价的授权安全属性（阈值授权、低于阈值保密、份额刷新）。
- **签名方案可替换**：迁移到后量子签名（如SLH-DSA、ML-DSA）只需轮换成员密钥，不改变协议逻辑，混合法定人数（不同签名方案共存）可行。
- **信息论安全性**：低于阈值时，印记系数信息论保密，对抗将来量子攻击不依赖代数假设。
- **HSM兼容**：成员签名密钥可放在商用HSM，只需标准签名API；执行层HSM可保护主密钥，升级为双因素定性多签。
- **可编程授权**：印记可扩展为更复杂策略（加权、层次化、机密累计限制等），且不影响成员签名方案。
- **效率**：每个成员仅需一次域运算和一次普通签名，非交互，轮次少。

## 7. 优点

- **签名无关性**：解锁了过去依赖TSS的强耦合，使后量子迁移成为参数更换，大幅降低风险。
- **HSM原生支持**：与现有HSM基础设施无缝集成，无需定制硬件或固件。
- **安全性信息论**：低于阈值时的保密性不依赖计算困难假设，抗量子。
- **程序化授权能力**：秘密共享状态支持机密策略评估，超出简单计数。
- **非交互且低延迟**：批准阶段无需成员间通信，授权阶段一轮收集即可。
- **无长期联合代数密钥**：不公布群公钥，避免未来量子破解目标。
- **形式化安全保证**：给出了严格的归约证明和具体安全边界。

## 8. 不足与局限

- **输出非原生签名**：产物是执行层授权印记，不能直接用于任意区块链原生验证器，需要自定义验证逻辑。若需兼容遗留系统，需依赖HSM作为主密钥守卫。
- **自定义验证逻辑要求**：执行层必须实现签名验证、域运算、哈希、有序最终性等组件；无法直接在单纯签名检查的链上部署。
- **单次使用系数槽**：每个操作消耗一个槽，槽的预共享和状态维护增加操作复杂度；槽耗尽影响活性。
- **设置要求较高**：需要VSS/AVSS/JRSS以支持每个成员的份额打开信息，某些现有设置可能不直接满足。
- **法定人数可见性**：接受操作的法定人数成员身份对执行层公开，而某些阈值签名可隐藏法定人数。
- **经济性捕获**：贿赂或胁迫t个成员仍可攻破系统（与TSS、多签相同），无法防御此类社会工程攻击。
- **实验验证不足**：缺乏与TSS协议的定量比较、大规模异步网络仿真、设置协议的具体实例化验证（开放问题）。
- **安全模型**：假设静态腐败，未处理自适应腐败；模型假设至少一个设置参与者诚实。

（完）

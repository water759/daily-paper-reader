---
title: Degree-Constrained Interval Optimization for Minimax Polynomial Approximation in Homomorphic Encryption
title_zh: 同态加密中极小化极大多项式逼近的度数约束区间优化
authors: "Jiheon Woo, Donggyun Ryu, Yongjune Kim"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08042v1"
tags: ["query:sec-cloud-ai"]
score: 8.0
evidence: 同态加密中的多项式近似优化，支持机密计算
tldr: 同态加密（HE）下非多项式激活函数需用多项式近似，minimax近似（Remez算法）最小化给定区间上的最大误差，但区间宽度与精度存在权衡。本文将区间选择建模为分布感知优化，以最小化相对于预激活分布的均方误差（MSE），并通过域扩展函数（DEF）及其HE可实现的多项式变体（DEP）抑制区间外误差。推导了基于DEF的代理目标及DEP实现的误差上界，实现了精度与鲁棒性的更好权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08042v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 651, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 798, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 776, \"height\": 2106, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 761, \"height\": 2097, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08042v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 760, \"height\": 1347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08042v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1582, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08042v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1613, \"height\": 556, \"label\": \"Table\"}]"
motivation: HE中minimax多项式近似的区间选择未考虑输入分布，导致区间外误差不可控，需联合优化区间与域扩展以最小化实际MSE。
method: 提出分布感知的区间优化，将minimax区间选择转化为MSE最小化问题，结合DEF/DEP控制区间外误差，并导出代理目标及实现误差上界。
result: 推导了基于DEF的解析代理目标及DEP实现的误差分解与上界，连接了理想优化与HE可实现方案。
conclusion: 通过区间与域扩展联合优化，在HE多项式近似中实现了更优的精度-鲁棒性权衡。
---

## 摘要
同态加密（HE）在算术约束下实现隐私保护推理，这些约束将加密计算限制为加法和乘法。因此，非多项式激活函数必须用多项式逼近替代。在多项式逼近方法中，极小化极大逼近（通常由Remez算法计算）是一种标准方法，因为它能在给定设计区间上最小化最大逼近误差。对于极小化极大多项式设计，逼近区间是一个关键超参数：较宽的区间能提高对大幅值输入的鲁棒性，但会在固定度数预算下增加极小化极大逼近误差。本文将这种权衡建模为一个分布感知的区间优化问题，其中选择逼近区间以最小化相对于感兴趣预激活分布的均方误差（MSE）。为了有效控制区间外输入，我们将极小化极大多项式与域扩展函数（DEF）及其可实现HE的多项式对应物——域扩展多项式（DEP）相结合，这些多项式在设计区间外近似限幅操作，从而抑制不可控的多项式外推。我们首先推导出一个解析上可处理的基于DEF的代理目标，该目标捕捉了区间内极小化极大逼近误差与区间外限幅误差之间的权衡。然后，我们通过实现误差分解及其伴随的上界，将这个理想化目标与可实现HE的DEP结构联系起来。

## Abstract
Homomorphic encryption (HE) enables privacy-preserving inference under arithmetic constraints that restrict encrypted evaluation to additions and multiplications. As a result, non-polynomial activation functions must be replaced by polynomial approximations. Among polynomial approximation methods, minimax approximation, typically computed by the Remez algorithm, is a standard approach because it minimizes the maximum approximation error over a given design interval. For minimax polynomial design, the approximation interval is a critical hyperparameter: a wider interval improves robustness to large-magnitude inputs while increasing the minimax approximation error under a fixed degree budget. In this paper, we formulate this trade-off as a distribution-aware interval optimization problem, where the approximation interval is chosen to minimize the mean-squared error (MSE) with respect to the pre-activation distribution of interest. To effectively control outside-interval inputs, we combine minimax polynomials with domain extension functions (DEFs) and their HE-realizable polynomial counterparts, domain extension polynomials (DEPs), which approximate a clipping operation outside the design interval and thereby suppress uncontrolled polynomial extrapolation. We first derive an analytically tractable DEF-based proxy objective that captures the trade-off between within-interval minimax approximation error and outside-interval clipping error. We then connect this idealized objective to HE-realizable DEP constructions through an implementation-error decomposition with an accompanying upper bound.

---

## 论文详细总结（自动生成）

# 论文总结：同态加密中极小化极大多项式逼近的度数约束区间优化

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：同态加密（HE）支持对加密数据直接进行计算，但只允许加法和乘法等算术操作，因此神经网络中的非多项式激活函数（如ReLU、sigmoid等）必须用多项式逼近替换。
- **问题**：极小化极大多项式逼近（Minimax approximation）通常用于最小化给定区间上的最大逼近误差。但逼近区间是一个关键超参数：区间越宽，对大幅值输入的鲁棒性越好，但在固定多项式度数下会增大区间内的最大误差；区间太窄则会导致区间外多项式外推产生巨大误差（如ReLU在区间外误差可达数千倍）。
- **现有不足**：传统方法仅固定设计区间，未考虑输入分布；单纯扩大区间会牺牲区间内精度。已有工作提出域扩展函数（DEF）和域扩展多项式（DEP）用于控制区间外行为，但未解决区间选择的最优化问题。
- **整体目标**：将区间选择建模为分布感知的优化问题，结合DEF/DEP，在给定多项式度数下最小化实际预激活分布的均方误差（MSE），实现精度与鲁棒性的更好权衡。

## 2. 方法论：核心思想与技术细节
- **核心思想**：将逼近区间 \([-t, t]\) 作为优化变量，定义基于DEF的理想限幅目标 \(J_{\text{DEF}}(t)\)，并推导一个解析可处理的代理目标 \(\hat{J}_{\text{DEF}}(t)\)，然后通过DEP实现HE兼容的区间控制，并给出实现误差的上界。
- **关键技术细节**：
  1. **DEF代理目标**：利用极小化极大残差的等波纹特性，将区间内误差建模为弧弦分布（Arcsine distribution），从而得到区间内误差项 \(\hat{J}_{\text{DEF,in}}(t) = \frac{E_n(t)^2}{2} \Pr(|X| \le t)\)；区间外误差通过端点值替换导出解析形式 \(\hat{J}_{\text{DEF,out}}(t)\)。
  2. **类型特定处理**：
     - 对于正齐次函数（如ReLU、LeakyReLU），利用缩放性质 \(E_n(t) = t E_n(1)\) 得到解析最优半径公式（Theorem 1）。
     - 对于非齐次函数（sigmoid、tanh、GELU），通过采样若干t的极小化极大误差并拟合光滑代理函数 \(\tilde{E}_n(t)\) 来近似。
  3. **DEP实现与误差界**：用DEP多项式组合代替硬限幅DEF，推导实现误差 \(J_{\text{impl}}(t)\) 的上界（Proposition 2），并通过三角不等式关联 \(J_{\text{DEF}}\) 和 \(J_{\text{DEP}}\)。
  4. **最优半径求解**：对分布假设为高斯或拉普拉斯，通过一阶条件或数值搜索得到最优 \(t^*\)。

## 3. 实验设计
- **数据集/场景**：使用合成数据——预激活分布假设为高斯分布 \(N(0,1)\) 和拉普拉斯分布 \(\text{Laplace}(0, 1/\sqrt{2})\)（均为单位方差）。未使用真实数据集。
- **Benchmark**：固定区间基线（Fixed-interval Remez）——在保守大区间 \([-20\sigma, 20\sigma]\) 上直接使用相同度数的极小化极大多项式（无DEP）。
- **对比方法**：
  - 解析代理目标 \(\hat{J}_{\text{DEF}}\)
  - 数值理想DEF目标 \(J_{\text{DEF}}\)（直接计算积分MSE）
  - 两种DEP实现：\(L_1 = 1.6\) 和 \(L_2 = 2.5\) 的扩展因子对应的 \(J_{\text{DEP},L_1}\) 和 \(J_{\text{DEP},L_2}\)
- **测试函数**：ReLU、LeakyReLU（坡度0.01）、sigmoid、tanh、GELU，度数 \(n=15\)。

## 4. 资源与算力
- 文中**未明确说明**使用的GPU型号、数量或训练时长。所有数值实验通过Chebfun工具在CPU上完成极小化极大多项式计算，未涉及深度学习训练。

## 5. 实验数量与充分性
- **实验数量**：对5种激活函数，每种在2种分布（高斯、拉普拉斯）下进行，共10组对比场景。每组呈现了最优半径和MSE值（Table I & II），以及部分函数MSE随t变化的曲线（Fig.3-5）。
- **充分性分析**：
  - 覆盖了主流激活函数类型（齐次与非齐次）。
  - 对比了理想目标、代理目标及两种DEP实现，验证了代理目标能准确找到最优半径。
  - 但缺少真实神经网络端到端实验（如CIFAR-10推理精度），仅使用合成分布模拟；也未对比其他区间选择方法（如基于Chebyshev多项式逼近的误差界方法）。实验整体充分但偏理论和模拟，公平性主要体现为与固定区间基线的对比。

## 6. 主要结论与发现
- 所提出的解析代理目标 \(\hat{J}_{\text{DEF}}\) 能很好地匹配数值理想目标 \(J_{\text{DEF}}\) 的最优半径，尤其在最优值附近吻合度高。
- 相对固定区间Remez基线，优化的DEP实现（特别是 \(L=2.5\)）能显著降低MSE，对sigmoid、tanh、GELU的改进可达数个数量级。
- 较小扩展因子（\(L=1.6\)）需要更多DEP阶段，累积误差更大，使得实际MSE略高于 \(L=2.5\) 的情况。
- 拉普拉斯分布的重尾性质导致最优半径大于高斯分布，但代理目标仍能准确预测。

## 7. 优点
- **理论创新**：首次将区间优化与分布感知结合，并给出解析可处理的代理目标和实现误差上界，理论推导严谨。
- **方法通用性**：适用于齐次和非齐次激活函数，且对任何连续分布（通过数值积分）均可扩展。
- **实践关联**：从DEF理想模型连接到HE可实现的DEP多项式，给出了实现间隙的量化分析，有助于指导实际系统设计。
- **计算高效**：最优半径可通过一次求解或简单数值搜索得到，无需复杂训练。

## 8. 不足与局限
- **实验覆盖有限**：仅使用合成分布（高斯/拉普拉斯），未在真实神经网络预激活分布上验证；缺少端到端HE推理的精度实验。
- **未考虑层间累积误差**：实际网络中多个非多项式层的近似误差会叠加，本文仅分析单层MSE。
- **DEP参数未优化**：仅固定了两种扩展因子（1.6和2.5），未对δ、γ等参数进行调优，也未讨论给定HE成本约束下的最优L选择。
- **未与最新方法对比**：如基于Chebyshev逼近或MiniONN等HE友好逼近策略，也缺乏与自适应区间方法的比较。
- **资源/算力未报告**：缺少实验硬件和计算时间的量化，影响可复现性评估。

（完）
